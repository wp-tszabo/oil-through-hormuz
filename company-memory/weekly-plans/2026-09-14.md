# Weekly Plan: Week of 2026-09-14

**Status**: APPROVED

## Phase

Phase 1: free/public data source only, no paid subscriptions, no
monetization work, all copy held for owner review before publishing.

## Objectives this week

- OKR Phase 1 / KR1: identify and validate a free, ToS-clear source for
  Strait of Hormuz oil-flow data.
- OKR Phase 1 / KR2: get a minimal site skeleton live on GitHub Pages
  (structure only is fine this week; live data can land once Research
  confirms a source).
- OKR Phase 1 / KR3/KR4: no publishing without an owner copy-approval and a
  logged self-check — this week is about proving the process works, not
  about speed.

## Planned work

| Owner | Work item | Notes |
|---|---|---|
| Research | Survey free/public Hormuz oil-flow data sources (start with EIA), check each candidate's terms of service for redistribution rights, recommend one primary source and the update cadence it realistically supports. | See GOVERNANCE.md critical-issue #3 — treat ToS as guilty until checked. Log findings + the licensing check in `decisions-log.md`. |
| Build | Scaffold the static site (already stubbed at `site/`) and get GitHub Pages deploy working end-to-end with placeholder content. Wire in real data once Research delivers a recommendation. | Do not connect a data source Research hasn't cleared. |
| Monetization | Dormant. | No work this phase. |
| CEO | Draft homepage copy (headline + short methodology/about blurb) and hold it for owner review. Run the self-check rubric before anything goes live. Keep `backlog.md`, `metrics.md`, `decisions-log.md` current. | Copy must not be published without explicit owner approval, per the standing checkpoint. |

## Spend ceiling requested

**~$0** — free-tier data source, GitHub Pages hosting (free), no paid tools
needed this week. This is the Phase 1 baseline agreed with the owner.

## Risks / things that could trigger a critical-issue interrupt

- A candidate data source's ToS turns out not to permit redistribution —
  treat as a legal/compliance critical issue, don't just pick another source
  quietly; log it.
- If GitHub Pages setup requires anything outside the $0 ceiling (it
  shouldn't), pause and raise it rather than assuming it's fine.

## Owner decision

- [x] Approved as-is
- [ ] Approved with amendments (noted below)
- [ ] Rejected / needs rework

Approved 2026-09-16 by owner (in conversation, not GitHub issue comment —
GitHub-based async approval flow can be used for future weeks, but this
first approval happened directly). Hosting decision made same day: repo
made public, GitHub Pages enabled (see decisions-log.md).

## Execution log

### Cycle 2026-09-17 (mid-week — plan still open, NOT done)

Status of the plan remains `APPROVED` and in execution. The week does not
close until 2026-09-20; this cycle is a mid-week execution pass.

**Done this cycle**

- Critical-issue check run first, as required. One issue raised: GitHub
  issue #1 (`critical`) — the CEO's egress policy blocks every data-source
  domain *and* the live site, which makes both the ToS clearance required by
  critical-issue category 3 and the accuracy spot-check required by
  rubric §1 impossible. Logged in `critical-issues-log.md`. Awaiting an owner
  decision between three options.
- Research pass (reduced depth, run by the CEO — see below): candidates
  identified and logged in `decisions-log.md`. **No source cleared, none
  recommended.**
- Homepage copy drafted and held: `company-memory/pending-copy/2026-09-17-homepage.md`,
  opened for owner review as issue #2 (`needs-copy-review`). Contains no
  figures, deliberately.
- Rubric run twice and logged in `self-check-log.md`: PASS on the copy draft
  (as a draft; publish still blocked), INCONCLUSIVE on the live site.
- Async approval mechanics stood up for real — labels created, first two
  issues filed. Confirmed working auth path is REST against `api.github.com`;
  the `gh` CLI assumed by GOVERNANCE.md is not present in this sandbox.
- Memory updated: `okrs.md`, `backlog.md`, `decisions-log.md`, `metrics.md`,
  `self-check-log.md`, `critical-issues-log.md`.

**Deliberately not done**

- No data source wired. No commit touches `site/`, so no Pages deploy fired
  and nothing reached the public site.
- No spend. $0 agent-committed, within the ~$0 ceiling.
- No Phase 2 or monetization work.

**Deviation from the plan, for the record**

The plan assigns work to Research and Build. The Agent/subagent tool was not
available in this session, so **nothing was delegated** — the CEO did a
reduced-depth research pass itself and did no Build work. This is a deviation
from the approved approach and is flagged rather than absorbed: it means
there was no independent specialist output for the CEO to review, which is
part of how this company was designed to catch its own mistakes.

### Cycle 2026-09-17b (second cycle same day — plan still open, NOT done)

Status remains `APPROVED` and in execution. Week closes 2026-09-20.

**The headline: the environment changed and the core research question got answered.**

- Critical-issue check run first, as required. **Two findings.**
  1. **Egress restored.** Every domain that hard-denied in the morning cycle
     (`eia.gov`, `portwatch.imf.org`, `imf.org`, `wp-tszabo.github.io`) now
     returns HTTP 200. No owner action caused this; it changed underneath us.
     Issue #1 updated, left open for its residual asks.
  2. **NEW CRITICAL — HTTPS is broken on the custom domain.** `https://oilthroughhormuz.com`
     and both `www` forms fail TLS (Pages serving a `*.github.io` cert, custom-domain
     cert never provisioned); `http://www.…` 301s *into* the broken URL. Only
     apex-over-HTTP and the `github.io` URL work. Raised as issue #3, `critical`.
     This had been sitting in the backlog as a routine "enable HTTPS enforcement"
     task — it is not routine, it is the primary domain being unusable. Notably,
     the previous cycle's egress block had been **masking a real outage**: "can't
     fetch our own site" was read as a proxy rule and was partly a genuine defect.

- **Licence verdict delivered — IMF PortWatch REJECTED.** Verified first-hand:
  all PortWatch datasets are `license: custom` → IMF terms permitting only
  non-systematic personal/noncommercial downloading "without any right to resell,
  redistribute, compile, or create derivative works". Our intended use is all four.
  **Nothing was ever ingested** — the guilty-until-checked rule worked exactly as
  designed and saved us building on a feed we'd have had to rip out.
- **EIA confirmed clear** (US public domain, attribution requested) but its Hormuz
  chokepoint figures are annual/half-yearly; the freshest (20.9 m b/d, 1H25) covers
  a period that ended ~14 months ago, while Q1-2026 flows are reported ~30% lower
  amid regional conflict. So: a source we may publish, and no source fresh enough
  for the product as conceived.
- **Re-scope escalated, not decided** (issue #1): (A) ask the IMF for written
  permission, $0; (B) re-scope honestly around EIA; (C) Phase 2 paid data.
  Recommended A+B, with C explicitly withheld until A is tried — a budget ask
  before sending a free email would be the default-to-spending that GOVERNANCE.md
  forbids. **No email sent**; outward contact is the owner's to authorise.
- **Live-site self-check upgraded from INCONCLUSIVE to a real PASS** — fetched both
  live URLs, diffed byte-identical against the repo, confirmed zero data figures
  are live. Logged in `self-check-log.md`.
- Copy still held. Issue #2 unanswered; added a comment flagging that Draft C's
  "weeks old" caveat is now materially too generous, with proposed replacement
  wording and a recommendation to hold the draft until the re-scope lands.
- Memory updated: `okrs.md`, `backlog.md`, `decisions-log.md`, `metrics.md`,
  `self-check-log.md`, `critical-issues-log.md`.

**Deliberately not done**

- No data source wired, no commit touching `site/`, no Pages deploy fired, nothing
  published. $0 spend. No Phase 2 or monetization work. No DNS/CNAME changes
  (hard-to-reverse). No IMF contact. No EIA account registered in the owner's name.

**Deviation from the plan, again, for the record**

The Agent/subagent tool was unavailable for the **second consecutive cycle**, so
Research and Build again received nothing. The CEO did the research itself. This is
now a pattern rather than a glitch, and it matters for the Phase 1 evaluation: the
PortWatch rejection is a consequential call made with no independent specialist
output to check it against. The company has not yet once run as designed.

### Cycle 2026-09-17c (third cycle same day — plan still open, NOT done)

Status remains `APPROVED` and in execution. Week closes 2026-09-20.

**The headline: the company had been reasoning from a wrong fact, and correcting it made the product viable.**

- Critical-issue check run first, as required. **Three findings.**
  1. **Issue #3 (broken HTTPS) RESOLVED and closed.** The owner re-saved the custom
     domain and certificate issuance completed. Verified before closing rather than
     accepted on report: valid Let's Encrypt cert for `oilthroughhormuz.com`, HTTPS
     enforced, and all four entry routes now terminate on the valid-TLS apex.
     Uptime DEGRADED → UP.
  2. **NEW CRITICAL, pre-emptive (issue #5).** The owner's page spec asks for a
     headline figure dated to yesterday. No source we may legally republish measures
     Hormuz daily, so that figure would present a quarterly-anchored model output as
     a measurement — category 4, rubric FAIL. **Not published, not designed around**;
     put to the owner as a decision in PR #4 with three options.
  3. **NEW CRITICAL, near-miss on company memory.** The working tree was on a
     detached HEAD; `main` and `origin/main` were still at the *first* cycle's commit.
     Two cycles of decisions — including the PortWatch legal rejection — existed only
     on unpushed local commits. Recovered, verified to touch no `site/` files,
     fast-forwarded onto `main`, pushed. New standing end-of-cycle check adopted.

- **Prior finding overturned: EIA publishes Hormuz flows QUARTERLY, not annually.**
  The 2nd cycle's "~14 months stale" conclusion was based on the wrong EIA product.
  EIA's Global Energy Security Data supplement (launched 2026-05-13, latest release
  2026-08-12) carries Hormuz by quarter: 20.9 / 21.0 / 21.3 / 21.6 / 14.9 / **4.9**
  m b/d for 1Q25–2Q26. Real staleness ~11 weeks. The company now has real, cleared,
  attributable figures for the first time.
- **New source found and rejected the same cycle**: the WTO Strait of Hormuz Trade
  Tracker is daily and exactly the right shape, but is AXSMarine commercial data under
  "© WTO 2026" with no open-data grant. Not ingested, not used for calibration.
- **Owner-directed estimation methodology delivered** — `company-memory/methodology.md`:
  input inventory, model, regime detector, and a real back-test (accurate to 1.4% in
  calm quarters; wrong by +45% and +204% across the disruption). Conclusion: the model
  works, but its current honest band (~1.5–7 m b/d) is wider than the quantity
  estimated, so no daily point figure. That judgement was escalated, not taken.
- **Homepage v2 built to the owner's simplified spec and held** — a real page
  (`site/index.html` + `site/data/hormuz.json`) on branch `copy-review/homepage-v2`,
  opened as **PR #4** (`needs-copy-review`). Deploy is `main`-only, so the branch
  cannot reach the live site.
- **Rubric run pre-publish and logged — and it caught a real error for the first time**:
  the draft asserted a next-release date that belongs to the parent STEO, not to the
  Hormuz dataset. Corrected in the page, the JSON and the methodology doc before the
  owner saw it.
- **CEO recommendation reversed on the IMF email**: no longer worth sending, because
  the gap it would close shrank and PortWatch's raw AIS is unreliable precisely now.
  Still the owner's decision; still unsent.
- Memory updated: `okrs.md`, `backlog.md`, `decisions-log.md`, `metrics.md`,
  `self-check-log.md`, `critical-issues-log.md`, plus new `methodology.md`.

**Deliberately not done**

- Nothing published. No merge to `main` of any `site/` change, so no Pages deploy fired.
- $0 spend. No Phase 2 or monetization work. No DNS/CNAME changes. No IMF contact.
- No EIA account registered in the owner's name.
- No graduation proposal — the Phase 1 evaluation notes in `okrs.md` explicitly decline
  to propose paid data while the honest free product is still awaiting a decision.

**Deviation from the plan, a third time**

The Agent/subagent tool was unavailable again, so Research and Build received nothing
for the third consecutive cycle. The CEO did the research, wrote the methodology and
built the page. Everything consequential this cycle — clearing one source, rejecting
another on legal grounds, back-testing a model, and judging that the owner's own
instruction should not be followed literally — was done by one agent with no
independent specialist to check it. `methodology.md` §8 says so in the document itself
rather than presenting as reviewed work.

### Cycle 2026-09-17d (fourth cycle same day — plan still open, NOT done)

Status remains `APPROVED` and in execution. Week closes 2026-09-20.

**The headline: the owner overruled the CEO, and that is the system working.**

- Critical-issue check run first, as required. **One finding, and it is a
  resolution rather than a new problem.** Issue #5 — the pre-emptive category-4
  interrupt raised last cycle, that a dated daily headline would present model
  output as a measurement — is **resolved**. The owner answered on PR #4:
  publish the daily estimate, put "estimated" in the title. The CEO had
  recommended the opposite. The escalation existed precisely so this call would
  be made by the owner, and it was.
- **Homepage v2 rebuilt to the decision, on the branch, not merged.** The
  headline is now the model's point estimate for 2026-09-16 (4.9 m b/d), with
  "estimate" in the `<title>`, the H1, the unit line and a standalone label, and
  the **1.5–6.9 m b/d band directly under the number**. The band was not part of
  the owner's instruction — `rubric.md` §1.6 requires a visible range for any
  modelled figure and nobody asked for that clause to be waived, so both ship.
  The interpretation is stated explicitly in `decisions-log.md` rather than
  quietly implemented, so the owner can reject it if it is wrong.
- **Two corrections the change forced, which are easy to miss**: the Terms
  paragraph described the figures as "third-party estimates" and stopped being
  true the moment the headline became our own model output; the Copyright
  paragraph didn't mention the model at all. Both fixed. Rubric §3.2 exists for
  exactly this class of drift.
- **Rubric re-run, full, not a diff** — PASS, logged. Included a fresh spot
  re-fetch of the EIA supplement (HTTP 200, "Release Date: August 12, 2026",
  Table 4 series identical to `hormuz.json`), an independent re-derivation of the
  band arithmetic, and a mechanical scan confirming every number on the page
  traces to the cleared series or to `methodology.md`.
- **A new risk created by the decision, named on the day it was created**: the
  page now carries a hard-coded date, so it goes stale by construction. Not a
  category-1 issue today — every word is true and the date is visible — but it
  becomes one if nothing re-dates it. Now the top Build item, and flagged to the
  owner on the PR rather than left in the backlog to be discovered.
- **A logging error found and corrected**: this cycle initially backfilled an
  issue-#5 row into `critical-issues-log.md` believing it was missing. It was
  not — it was already there, and the duplicate was removed and replaced with a
  proper resolution row. Recorded because a memory file that quietly grows
  duplicate history is a worse failure than a missing row.
- `methodology.md` §6 marked SUPERSEDED with a new §10 recording the decision and
  the published parameters; `hormuz.json` flips `daily_estimate_published` to
  true and records who authorised it.
- Memory updated: `decisions-log.md`, `critical-issues-log.md`, `self-check-log.md`,
  `backlog.md`, `methodology.md`, `pending-copy/2026-09-17-homepage-v2.md`.

**Deliberately not done**

- **Not merged.** The owner's comment resolved what the page says; it did not say
  "merge", and merging is publishing. Final go-ahead requested on PR #4.
- Nothing published, no Pages deploy fired, $0 spend, no Phase 2 or monetization
  work, no DNS/CNAME changes, no IMF contact.
- The band was **not** removed to match the owner's instruction more literally.
  That would have been a rubric waiver the CEO is not authorised to grant.

**Deviation from the plan, a fourth time**

The Agent/subagent tool was unavailable again, so Research and Build received
nothing for the fourth consecutive cycle. The specific cost this time: the
judgement that "estimated title + visible band" honours both the owner's
instruction and rubric §1.6 was made and reviewed by the same agent.

### Cycle 2026-09-17e (fifth cycle same day — plan still open, NOT done)

Status remains `APPROVED` and in execution. Week closes 2026-09-20.

**The headline: the company got its first standing product mandate, and the copy checkpoint survived the moment it was most likely to lapse.**

- Critical-issue check run first, as required. **No category 1/2/3 issue.** The
  live site was fetched directly: HTTP 200, valid TLS, served bytes identical to
  `main`. Spend $0. One **category-4 assessment** made and **resolved in design
  rather than raised as an interrupt** — see below and `critical-issues-log.md`.
- **Owner directive received on the now-live homepage, three parts**: make the
  explanation copy appealing; don't name an exact source in the visible trend
  copy, because the product is a proprietary aggregation method; move Terms and
  Copyright into footer links. Implemented on `copy-review/homepage-v3` and held
  as **PR #6** (`needs-copy-review`). **Not merged.** A copy change to an
  already-published page is still a copy change; the checkpoint is standing and
  does not lapse because v2 already shipped. This was the cycle where that rule
  was easiest to rationalise away — the change came from the owner's own
  instruction, on a page they had personally merged hours earlier.
- **The source-attribution question was resolved as a synthesis, in the open.**
  Stripping the attribution entirely would have implied we observe the strait or
  already blend many feeds — both false with one cleared input, and a category-4
  rubric FAIL. Refusing the directive would have been unwarranted. So: visible
  copy speaks of "our model"; the full attribution moves **intact** to a linked
  `sources.html`, which states outright that one cleared dataset is what is in
  the model today. The reasoning is written on PR #6 so the owner can overrule
  it, and a line is recorded in advance: if the owner wants *no* visible pointer
  to the source at all, that gets raised as a critical issue **before**
  implementation, not complied with silently.
- **New standing mandate: the CEO owns continuous improvement of the proprietary
  methodology** (owner's explicit words). Written into `okrs.md` as **KR6** — the
  company's first non-completable KR — plus `methodology.md` §11,
  `.claude/agents/ceo.md` and `.claude/agents/research.md`. The licence bar is
  restated at the point of temptation, because a mandate to add inputs is exactly
  the pressure under which guilty-until-checked gets quietly relaxed.
- **A standing backlog item was closed honestly**: the rubric was run against the
  **live** page as served, not against the repo or the branch. PASS, with the
  served-vs-`main` byte diff as evidence.
- **Rubric run twice, both PASS**, logged: once on the live page, once full (not a
  diff) on the v3 branch. Included a fresh source spot re-fetch — series and
  release date unchanged, and the parent-STEO date confusion has not crept back.
- Memory updated: `decisions-log.md` (5 entries), `okrs.md`, `backlog.md`,
  `metrics.md`, `self-check-log.md`, `critical-issues-log.md`, `methodology.md`,
  `pending-copy/2026-09-17-homepage-v3.md`, both agent definitions.

**Deliberately not done**

- **Not merged.** PR #6 is held for the owner. Nothing published, no deploy fired,
  the live page is unchanged by this cycle.
- $0 spend. No Phase 2 or monetization work. No DNS/CNAME changes. No IMF contact.
- No data change of any kind — v3 is copy and layout only.
- The 1.5–6.9 band was **not** moved behind a link with the rest of the footer
  text. Rubric §1.6 requires a *visible* range, and that is not the CEO's to waive
  while tidying a layout.

**Deviation from the plan, a fifth time — and now it has a specific cost**

The Agent tool returned "Task is disabled for this session, in subagents as well
as here." Two briefs were written in full and went undispatched: a Build brief for
the v3 pages (the CEO built them itself), and a Research brief that was to be the
**first pass of the new KR6 mandate** — scouting producer-side export series and
public-domain maritime advisories as a second anchor and a faster regime signal.
The owner has just made input-expansion the company's standing purpose, and the
specialist whose entire job that is cannot be reached. The Research brief is
preserved verbatim in `backlog.md` so it can be dispatched unchanged rather than
re-derived; KR6 therefore opens this cycle at **no progress**, recorded as such
rather than papered over.

### Cycle 2026-09-18 (sixth cycle — plan still open, NOT done)

Status remains `APPROVED` and in execution. Week closes 2026-09-20; this is the
last scheduled mid-week pass before close.

**The headline: a failure this company predicted in writing came true on schedule, and the checkpoint caught it.**

- Critical-issue check run first, as required, and it **found something** — the
  first time the opening check has produced a live, fired category-1 issue
  rather than a clean bill.
  1. **CRITICAL, category 1 (issue #7): the live estimate date was frozen at 16
     September while the page was being served on 18 September.** Not merely an
     old date: the methodology section promises the reader a figure *"carried
     forward to the day you are reading"*, so the page as a whole asserted
     something false. The 4th cycle had written down, on the day it created the
     risk, that this *would be* category 1 if nothing re-dated it. Nothing did.
     Raised before any other work, then fixed on live — **date only, no prose
     and no figure changed**, because persistence means the point estimate is
     4.9 for any day past the anchor and all three dates sit inside the same
     one-quarter band horizon. Rubric re-run: the pre-fix live page logged as a
     genuine **FAIL**, the fix as a **PASS**.
  2. **CRITICAL, category 4, pre-emptive and dated (logged, flagged in PR #8).**
     The band is back-tested one quarter past the anchor and no further. The
     anchor ends 2026-06-30, so **the horizon expires 2026-09-30 — twelve days
     out** — and EIA is not expected to publish 3Q26 until ~November. Past that
     date the site would be publishing an untested extrapolation wearing a
     tested-looking range.
- **The durable fix was built and deliberately NOT self-approved.**
  `scripts/refresh_estimate.py` + a daily Actions cron re-dates the estimate,
  re-anchors on each new EIA release, **scores the prior estimate against the
  new actual** (the §3 recalibration promise finally has a mechanism), and
  **fails loudly** — non-zero exit, nothing published — on any fetch or parse
  failure. It writes only inside `<!-- GENERATED:estimate -->` markers and never
  touches prose. Held in **PR #8**. The line drawn and logged: repairing today's
  content is CEO work; installing something that publishes tomorrow's content
  unattended is the owner's decision.
- **KR6 delivered for the first time, as (a) + (b) together.** Re-inventoried
  the EIA supplement instead of trusting that Table 4 was all it had — it has
  **ten** tables. Adopted **Table 2** (companion chokepoints + world total oil
  supply) as a cleared input at **zero new licence surface**, and used it to
  build **regime detector v2**, which distinguishes a Hormuz-specific disruption
  from a global one — impossible under the old single-series rule. 2Q26: Hormuz
  −67.1% while the no-Gulf-barrel control group *rose* 8.1% and world supply
  *rose* 3.7%.
- **The honest non-result, reported as such**: the published band **did not
  change** (still 1.5–6.9). Both disrupted quarters classify `local`, so there
  is no `systemic` history to calibrate a separate band against. A better
  diagnosis is not a better number, and `methodology.md` §12.3 says so in those
  words rather than presenting motion as progress.
- **The licence rule met its first real pressure from the mandate, and won.**
  **JODI-Oil REJECTED** — monthly producer-side Gulf export data, the exact
  "second anchor at better than quarterly cadence" the methodology names as the
  model's biggest weakness. Terms read in full: *"All such rights are
  reserved."* Not ingested, not used "to calibrate". Asking JODI for written
  permission is $0 and recommended, but is outward contact and therefore the
  owner's to send; nothing sent.
- Copy checkpoint enforced: `sources.html` changed because the inputs changed
  and that page promises to say what is in the model **today**. Held in PR #8
  (`needs-copy-review`) and written to
  `pending-copy/2026-09-18-sources-v2.md`. Care taken that "two tables" cannot
  read as a claim of breadth — Table 2 is described as a *control*, not an input
  to the headline.
- **An error caught in-cycle by the author, recorded rather than buried**: the
  first draft of the copy said "Table 5". It is Table 2. Fixed before the branch
  was pushed.
- Memory updated: `critical-issues-log.md`, `self-check-log.md` (3 runs),
  `decisions-log.md` (8 entries), `methodology.md` (§12), `okrs.md`,
  `backlog.md`, `metrics.md`, `pending-copy/`.

**Deliberately not done**

- **PR #8 not merged.** No automation is running, no cron is scheduled.
- No prose reached the live site. The only live change is a date.
- $0 spend. No Phase 2 or monetization work. No DNS/CNAME changes. No JODI or
  IMF contact. No EIA account registered in the owner's name.
- No graduation proposal.
- The band was **not** adjusted to make the methodology work look more
  productive than it was.

**Deviation from the plan, a sixth time**

The Agent tool returned *"Agent is disabled for this session, in subagents as
well as here."* Research and Build received nothing for the sixth consecutive
cycle. The specific cost this time is unusually clear: the JODI licence
rejection, the decision that a frozen date warranted fixing live without owner
review, and the judgement that the band should *not* move were all made and
reviewed by the same agent. The company has still never once run as designed,
and that belongs in the Phase 1 evaluation as a fact about the operating model,
not as a recurring footnote.

## End-of-week summary (filled in by CEO at close)

_(not yet — week closes 2026-09-20; next cycle should close the week, write this
summary, and propose the week of 2026-09-21)_
