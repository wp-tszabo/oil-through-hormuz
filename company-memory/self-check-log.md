# Self-Check Log

Append-only. One row per rubric run (see [rubric.md](rubric.md)). A `FAIL` on
any column means overall `FAIL` — log it anyway, don't skip logging failures.

| Date | Item checked | Accuracy | No copied text | On-purpose | Overall | Notes / evidence |
|---|---|---|---|---|---|---|
| 2026-09-17 (5th cycle) | **Homepage v3** — `site/index.html` rewritten + new `site/sources.html` and `site/terms.html`, on branch `copy-review/homepage-v3` (commit `8beeb33`; pre-publish check, **NOT live**, PR #6 not merged) | PASS | PASS | PASS | **PASS — publish gated on owner approval of PR #6** | **What this change is**: the owner's three directives of 2026-09-17 — appealing customer-facing copy, no named provider in the visible trend copy, Terms/Copyright behind footer links. Copy and layout only; **no figure, date, band or chart value changed**, verified by diffing the numeric content against `main`. **§1.1 (traceable, cited) — the clause this change actually touches, and the reason this is a full run rather than a diff.** The provider's name has moved off the home page into a linked `sources.html`. Judgement recorded as a judgement, not assumed: a citation reachable by an explicit in-context link from the chart it describes ("Where these come from →"), plus a second link in the methodology blurb and a third in the footer, satisfies "traces to a specific, cited source" the same way a footnote does. `sources.html` carries publisher, dataset, table, release date, licence status, the Vortexa provenance disclaimer and the publisher's AIS caveat — i.e. **the attribution moved intact, nothing was dropped**. Stated on PR #6 so the owner can overrule it. **§1.2 (spot re-fetch, don't trust cache)**: re-fetched `eia.gov/outlooks/steo/report/energysecurity/article.php` this cycle, HTTP 200 — Hormuz total oil `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9`, and the string "August 12, 2026" present. Matches `hormuz.json` and every figure rendered. Also re-confirmed that the September 9 / October 6 2026 dates on that page belong to the **parent STEO**, not to our supplement — the error the rubric caught in the 3rd cycle has not crept back, and v3 asserts no next-release date at all. **§1.3 (units/dates)**: unit line "million barrels per day"; estimate labelled for "Wednesday 16 September 2026", weekday verified against the calendar (2026-09-16 *is* a Wednesday) and consistent with today being Thursday the 17th; no intraday figure, so no time-zone ambiguity. **§1.4 (estimates labelled)**: "Estimated" in the `<title>` and the `<h1>`; the label above the figure reads "Our model's estimate for…"; the blurb says in plain words "This is our estimate, not an official measurement". The old four-places-in-red framing is gone, which was the owner's whole point, but the claim itself is not weaker — it is stated in prose the reader will actually read rather than in a disclaimer they will skip. **§1.5 (staleness surfaced)**: the blurb says the public record arrives a quarter at a time, months after the fact; `sources.html` names the anchor's coverage period, the release date, and the publisher's own warning that 2026 volumes are being revised frequently. **§1.6 (modelled figure) — checked clause by clause**: methodology published and linked (twice from the home page, plus the full working document in the public repo); **all inputs still independently cleared** — the only numeric input remains the public-domain quarterly series, PortWatch and WTO/AXSMarine remain rejected and were not used, not even to sanity-check; **visible uncertainty range retained on the main page** at body size under the figure ("Working range 1.5 to 6.9"). The range was deliberately **not** moved behind a link during the tidy-up — a range behind a link is not a visible range. **§2 (no copied text)**: all prose original to this cycle; the publisher's AIS caveat is paraphrased on `sources.html`, quoted verbatim only inside `company-memory/`; chart is our own inline SVG, reused byte-identical. Mechanically verified **zero** `<script>`, `<img>`, `<iframe>` on all three pages, and zero external requests (the only external hosts referenced are two hyperlinks, to the source page and the public repo). **§3.1 (on-purpose)**: serves the approved week-of-2026-09-14 plan's KR2/KR3 and the owner's directive; no monetization content anywhere. **§3.2 (authority not misrepresented) — the clause the source directive could have broken, examined directly.** Two failure modes were specifically looked for and are absent: the page nowhere implies we observe the strait ourselves, and nowhere implies the model already blends multiple feeds. "Proprietary" appears once, on `sources.html`, immediately above a section that states in those words "One cleared dataset, and we say so rather than implying a wider net". The aggregation ambition is confined to a "Where this is going" section framed as ongoing work. Independence from EIA/any government body is stated in full on `terms.html`. **§3.3**: $0 to produce. **§3.4 (standing copy checkpoint)**: **held, not published** — Pages deploys only on push to `main`, the branch is unmerged, PR #6 is labelled `needs-copy-review`. This row is not clearance to publish. **Mechanical numeric scan of rendered text, per page, no orphan numbers**: `index.html` → axis labels {0,5,10,15,20}, quarter labels {25,26}, cleared series {20.9, 21.0, 21.3, 21.6, 14.9, 4.9}, band {1.5, 6.9}, {3%}, and the date tokens {16, 2026}. `sources.html` → {4} (table number), {12, 2026} (release date), band {1.5, 6.9}, and back-test parameters {1.4%, 45%, 204%, 70%, 40%, 10%}, all traceable to `methodology.md` §4–5. `terms.html` → {2026} only. **One thing this check flags rather than clears**, unchanged from last cycle and not made worse by this PR: the estimate date is hard-coded and correct only today. Top Build item; restated on PR #6. |
| 2026-09-17 (5th cycle) | **The LIVE page** — `https://oilthroughhormuz.com/` as actually served (post-merge of PR #4; this closes the standing backlog item to verify the live page rather than assume it) | PASS | PASS | PASS | **PASS** | Run against what is actually on the wire, not against the repo. **Availability + transport**: HTTP 200, `ssl_verify_result=0` (valid certificate chain), `last-modified: Thu, 17 Sep 2026 15:26:20 GMT` — consistent with the PR #4 merge commit `63fa29d`. **Content identity**: served HTML diffed against `main`'s `site/index.html` ignoring whitespace — **identical**, so what is published is exactly what was reviewed and approved. That is the check the standing practice asks for; the previous cycle's PASS was against the branch, and a branch is not a deployment. **Accuracy**: paired with the fresh source re-fetch described in the row above — the live page's figures (20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9 and the 4.9 headline with its 1.5–6.9 band) all still match the source as published today. **Dating**: the live page reads "Wednesday 16 September 2026" and today is Thursday 17 September, so it is correctly showing yesterday's estimate and is **not** stale as of this check. Recorded precisely because that will stop being true without intervention — this PASS is timestamped, not open-ended. **No copied text / on-purpose**: unchanged from the pre-merge run; owner-approved copy, own SVG, no monetization content, $0. |
| 2026-09-17 (4th cycle) | Homepage v2 **after the owner's headline decision** — `site/index.html` + `site/data/hormuz.json` on branch `copy-review/homepage-v2` (pre-publish check; NOT live, PR #4 not merged) | PASS | PASS | PASS | **PASS — publish still gated on the owner's merge go-ahead** | **What changed since the previous row**: the headline is no longer EIA's measured quarter, it is the model's daily point estimate for 2026-09-16, per the owner's decision on PR #4 (comment `5716791659`). That flips the rubric's hardest clause, §1.6, from "not engaged" to "the central question", so this is a full re-run, not a diff. **§1.1/1.2 (traceable, spot re-fetched)**: re-fetched `eia.gov/outlooks/steo/report/energysecurity/article.php` this cycle, HTTP 200, and parsed Table 4 fresh — "Release Date: August 12, 2026", Hormuz total oil 20.9 / 21.0 / 21.3 / 21.6 / 14.9 / **4.9** for 1Q25–2Q26. Byte-for-byte agreement with `hormuz.json` and with every figure rendered on the page. Confirmed the 12 Aug supplement date is *not* the parent STEO's (the same fetch shows STEO release 2026-09-09, next 2026-10-06) — the error this check caught last cycle has not crept back. **§1.3 (units/dates)**: unit stated as "million barrels per day, estimated"; the estimate is dated to a named weekday ("Wednesday 16 September 2026", verified against the calendar); the anchor's coverage period is named in full; no intraday figure, so no time-zone ambiguity. **§1.4 (estimates labelled)**: the word "Estimated" is in the `<title>`, the `<h1>`, the unit line, and a standalone "Estimate — not a measurement" label above the number; the body says in plain words that nobody measures the strait daily in a way we may republish and that the figure is produced by our model rather than observed. **§1.6 (the modelled-figure clause) — the one that mattered, checked clause by clause**: methodology published and linked (yes, to `methodology.md` in the public repo, plus a four-paragraph plain-English summary on the page); all inputs independently cleared (yes — the *only* numeric input is the EIA public-domain series; PortWatch and WTO/AXSMarine remain rejected and were not used, not even to sanity-check the band); visible uncertainty range (yes — "between 1.5 and 6.9 million barrels per day" sits directly under the figure at body-text size, not in a footnote, and the copy explicitly instructs the reader to "treat the range as the answer and the single number as its midpoint"). Band arithmetic re-derived independently: −70%/+40% of 4.9 = 1.47/6.86 → 1.5/6.9. **§1.5 (staleness surfaced)**: the page states the anchor's period, that the model cannot see inside or after that quarter, and EIA's own caveat that Hormuz AIS data has been unreliable since end-February 2026. **No copied text**: all prose rewritten by the CEO this cycle; EIA's caveat paraphrased, verbatim quotation kept inside `company-memory/` only; chart is our own inline SVG — mechanically verified zero `<script>`, `<img>` or `<iframe>` tags. Mechanical numeric scan of the rendered text returns only {20.9, 21.0, 21.3, 21.6, 14.9, 4.9} (cleared series), {1.5, 6.9} (band), {1.4, 45, 204, 70, 40, 3, 10} (back-test errors and regime thresholds, all traceable to `methodology.md` §4–5), axis labels and dates. No orphan numbers. **§3.2 (authority not misrepresented) — the reason issue #5 existed**: the page now says the headline is *our own model estimate* and that no government or official body stands behind it; the Terms paragraph was amended because it previously described the figures as "third-party estimates", which stopped being true once the headline became ours; the Copyright paragraph now separates our model from EIA's public-domain series. Non-affiliation with EIA and any government body is stated. **§3.3**: $0 to produce. **§3.4**: held, not published — the Pages workflow fires only on push to `main`, the branch is not merged, and the owner's comment resolved the *content* question without saying "merge". **One issue this check raises rather than clears** (logged as a backlog item and flagged to the owner on the PR, not treated as a blocker): the page now carries a hard-coded estimate date, so it becomes stale by construction. Every word stays true, but a page reading "16 September 2026" a week from now is GOVERNANCE.md category-1 stale data. The check passes on the state being published today; the automated daily re-date is now the top Build item. |
| 2026-09-17 (3rd cycle) | Homepage v2 — draft page **with real figures for the first time**: `site/index.html` + `site/data/hormuz.json` on branch `copy-review/homepage-v2` (pre-publish check; NOT live, PR not merged) | PASS | PASS | PASS | **PASS as a draft — publish remains blocked pending owner approval** | **Accuracy**: every figure on the page traces to one cleared source, EIA Global Energy Security Data (release 2026-08-12), Table 4/Table 2, fetched and parsed directly from `eia.gov` this cycle rather than from search snippets. Mechanical check run over the rendered HTML: the only numeric text on the page is {20.9, 21.0, 21.3, 21.6, 14.9, 4.9} — exactly the cleared series — plus chart axis labels {0,5,10,15,20}. No number appears that is not in `hormuz.json`. Units ("million barrels per day") and coverage periods (calendar quarters, named in full as e.g. "April–June 2026") are explicit; no time-zone ambiguity since no intraday figure is shown. **§1.4/§1.6**: no modeled or derived figure is published at all — the headline is EIA's published quarterly average shown as published, so the daily-estimate conditions in §1.6 are not engaged. **§1.5 (staleness surfaced)**: this is the page's most prominent feature, not a footnote — the period line dates the figure, a note directly under the headline states it is not yesterday's number and why, and the footer carries EIA's own caveat that Hormuz AIS data has been unreliable since end-February 2026 and 2026 volumes are being revised. **One error caught and fixed by this check**: the draft initially said "the next update is due 6 October 2026" — that is the parent STEO's next-release date, not the Global Energy Security Data supplement's, which EIA does not publish. Corrected to "updated quarterly" in the page, the JSON and the methodology doc before this row was written. **No copied text**: all prose original to the CEO this cycle; EIA's AIS caveat is paraphrased in our own words in the page body and quoted verbatim only inside `company-memory/`, not on the page; the chart is an inline SVG we generate from the numbers (zero `<img>`, `<script>` or `<iframe>` tags on the page — verified mechanically), so §2.3 is satisfied by construction. **On-purpose**: serves KR1/KR2 under the approved week-of-2026-09-14 plan and the owner's 2026-09-17 page spec; no monetization content; $0 to produce. §3.2: the page states it is an independent project not affiliated with or endorsed by EIA or any government body, and attributes the analysis to EIA while explicitly disclaiming that we hold or license Vortexa's underlying data. §3.4 satisfied as **held, not approved** — opened as a PR labelled `needs-copy-review`; the Pages workflow triggers only on push to `main`, so the branch cannot reach the live site. **This row is not clearance to publish.** |
| 2026-09-17 | Draft homepage copy, `company-memory/pending-copy/2026-09-17-homepage.md` (pre-publish check; NOT live) | PASS | PASS | PASS | PASS (as draft — publish still blocked) | **Accuracy**: draft contains zero figures by design; every live-number slot is a `[FIGURE: …]` placeholder, so there is nothing to trace or spot-re-fetch. Draft B builds source + coverage period + retrieval timestamp into the figure block, and Draft C commits to surfacing staleness and to naming AIS jamming/spoofing/dark-vessel uncertainty — satisfying rubric 1.4 (estimates labelled as estimates) and 1.5 (gaps surfaced). **No copied text**: all prose written by the CEO this cycle; no quoted material, so no attribution needed; no third-party charts or images. **On-purpose**: the week-of-2026-09-14 approved plan names "Draft homepage copy (headline + short methodology/about blurb) and hold it for owner review" as a CEO item. No monetization content. Drafts C/D/E explicitly disclaim official authority and state non-affiliation (rubric 3.2). $0 to produce (3.3). Rubric 3.4 (owner review before live) is **satisfied as "held, not yet approved"** — sent to owner as GitHub issue #2, label `needs-copy-review`; it must not be published until that issue is approved, and this row must not be read as clearance to publish. |
| 2026-09-17 (2nd cycle) | Live site, **actually fetched this time** — `https://wp-tszabo.github.io/oil-through-hormuz/` and `http://oilthroughhormuz.com/` | PASS | PASS | PASS | **PASS** (supersedes the INCONCLUSIVE row below) | Egress restored, so the spot-check the rubric asks for was finally performable. **Accuracy**: fetched both live URLs and diffed against the repo's `site/index.html` — byte-identical ignoring whitespace, so what is deployed is what is reviewed. Scanned the served HTML for numeric content: the only digits present are CSS values (`0.2rem`, `640`, `999px`, `#16181a`, etc.). **Zero data figures are live**, so there is no number to trace, no unit/timezone ambiguity, no unlabelled estimate, and no staleness to surface — rubric §1.1–1.5 pass vacuously but genuinely. **No copied text**: prose is the CEO's original placeholder; no quotes, no third-party charts or images. **On-purpose**: serves KR2 (site skeleton live); page self-labels "Phase 1 — under construction", claims no authority, links the public governance repo; no monetization content; $0 to produce. §3.4 satisfied — nothing on the live page is copy pending review; the drafts in `pending-copy/` are still held. **Caveat recorded, not hidden**: this PASS covers *content correctness*, not availability. The site is simultaneously **degraded** — HTTPS on the custom domain fails TLS (critical issue #3, GitHub issue #3). The rubric has no availability column, so that is tracked as a critical issue rather than silently folded into this PASS. |
| 2026-09-17 | Live site (`site/index.html` as deployed) — attempted verification | NOT VERIFIABLE | PASS | PASS | INCONCLUSIVE — logged, not treated as a pass | Could not fetch `oilthroughhormuz.com`, `www.oilthroughhormuz.com` or `wp-tszabo.github.io`: egress policy denial (see critical issue #1). Indirect evidence the site is up: apex DNS resolves to 185.199.108–111.153 (GitHub Pages), `www` CNAMEs to `wp-tszabo.github.io`, last Pages deploy (`3fd9200`) succeeded 2026-09-16T20:11Z. Reviewed the repo copy of `site/index.html` instead: contains no figures, labels itself "Phase 1 — under construction", claims no authority — so nothing currently live can be inaccurate about data. Recorded as INCONCLUSIVE rather than PASS because inference from DNS is not the spot-check the rubric asks for. |
| 2026-09-18 (6th cycle) | **The live page as served at the start of this cycle** — `https://oilthroughhormuz.com/`, pre-fix, headlining "estimate for Wednesday 16 September 2026" on 18 September | **FAIL** | PASS | **FAIL** | **FAIL** | Logged as a failure rather than skipped, per this log's own header. Fetched live: HTTP 200, `ssl_verify_result=0`, served bytes identical to `main`. **§1.3 (dates unambiguous and correct) FAIL** — not because the label was a lie in isolation (the estimate genuinely was for 16 September) but because the page pairs it with an explicit promise in the methodology section: *"carries it forward to the day you are reading."* Read together, the page told an 18-September visitor it was showing them an 18-September figure. **§3.2 (does not misrepresent the data's authority) FAIL** for the same reason — the claim made about the figure's currency exceeded what the figure was. §1.1, §1.2, §1.4, §1.6 all PASS: the number still traced to the cleared EIA anchor, the band was still visible, the estimate was still labelled an estimate. §2 all PASS, no prose issue. **Consequence applied**: per GOVERNANCE.md a FAIL on live content triggers remediation plus a critical-issue entry — both done, critical issue #7 and the re-date below. |
| 2026-09-18 (6th cycle) | **The re-date fix** — `site/index.html` + `site/data/hormuz.json` moved to Thursday 17 September 2026 (pre-publish check; goes live when this cycle's commit is pushed) | PASS | PASS | PASS | **PASS** | **§1.1** every number still traces to the cleared EIA anchor with `retrieved_utc` in `hormuz.json`. **§1.2 spot re-fetch done today, not trusted from cache**: `eia.gov/outlooks/steo/report/energysecurity/article.php` HTTP 200, Table 4 Hormuz total oil `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9` — identical to what is published. **A real trap re-checked and avoided**: the page's top banner now reads "Release Date: September 9, 2026", but that is the **parent Short-Term Energy Outlook**; the Global Energy Security Data supplement's own banner still reads "Release Date: August 12, 2026". This is precisely the confusion the rubric caught on 2026-09-17, so it was verified by matching on the supplement heading rather than the first date on the page. `hormuz.json` `release_date` correctly left at 2026-08-12. **§1.3** the clause that failed above, now correct: 2026-09-17 independently confirmed to be a Thursday. **§1.4/§1.6** unchanged and still PASS — "estimate" in the title, H1, unit line and label; band 1.5–6.9 visible under the figure. Band verified unchanged on purpose: persistence means the point is 4.9 for any day past the anchor, and 16/17/18 Sep sit in the same one-quarter-ahead horizon, so **no figure moved** — this was a date correction, not a new estimate. **§2** no prose changed at all. **§3.4 (owner copy checkpoint)** PASS *because* no new prose reached live: every word on the live page remains the v3 text the owner merged in PR #6, and all new copy this cycle is held in PR #8. |
| 2026-09-18 (6th cycle) | **PR #8** — `scripts/refresh_estimate.py`, daily Actions cron, companion-chokepoint input, regime detector v2, horizon guard, `sources.html` copy (pre-publish, **NOT live**, branch `model/companion-chokepoints-and-daily-refresh`, commit `d13ba39`) | PASS | PASS | PASS | **PASS — publish gated on owner approval of PR #8** | **§1.1/§1.2** the new companion series come from **Table 2 of the same EIA supplement already cleared** — same publication, same US-federal public-domain licence, no new licence surface at all. Values parsed today from the live page and cross-checked by hand: Malacca `22.3/23.8/23.4/24.9/21.3/16.6`, Bab el-Mandeb `3.9/4.5/4.6/5.4/5.6/8.1`, control group (Danish+Turkish+Panama) `11.6/11.0/11.4/11.5/11.1/12.0`, world supply `105.0/107.7/108.2/103.9/96.1/99.7`. **§1.4/§1.6 — the clause this change is really about.** The script was run against live EIA data and **reproduces the currently published figures exactly** (point 4.9, band 1.5–6.9, regime disrupted), which is the evidence that the automation is faithful to the hand-built page rather than a silent re-derivation. Both non-nominal paths were tested: horizon-exceeded correctly suppresses the point estimate and renders the "no current estimate" block; a bad source URL correctly exits **non-zero and publishes nothing**. **§1.5 (staleness surfaced, not hidden)** — materially strengthened: the horizon guard makes the model refuse to answer past its back-tested evidence instead of serving an untested range. **§3.2** the honest limit is stated rather than glossed — with only two disrupted quarters, both classifying `local`, there is not yet enough history for a separate local/systemic band, so **the published band does not change** and both the PR and `methodology.md` §12 say so in those words rather than presenting a better detector as a better number. **§3.4** `sources.html` prose changed, so this went to the owner as a labelled `needs-copy-review` PR and is also written to `pending-copy/2026-09-18-sources-v2.md`. Copy checked against the standing rule that the page must describe what is in the model **today**: Table 2 is described as a *control*, explicitly not as an input to the headline, so "two tables" cannot read as a claim of breadth we do not have. **One error caught in-cycle by this check**: the first draft of the copy said "Table 5"; it is Table 2. Corrected before the branch was pushed. |
| 2026-09-19 (7th cycle) | **Live page as served, BEFORE any change** — `https://oilthroughhormuz.com/`, opening critical-issue check | **FAIL** | PASS | PASS | **FAIL** | **Second consecutive daily FAIL on the same clause, and logged as a FAIL rather than absorbed as known-maintenance.** §1.3 (dates unambiguous *and correct*): headline read "Our model's estimate for **Thursday 17 September 2026**" while the page was being served on **Saturday 19 September 2026** — two days stale. §1.5 (staleness surfaced, not hidden): worse than hidden, actively contradicted — the methodology section promises a figure *"carried forward to the day you are reading"*, so the page asserted something false about itself for the second day running. Availability itself was fine and is recorded separately so the two are not conflated: HTTP 200, `ssl_verify_result=0`, served bytes **identical** to `main`'s `site/index.html` (whitespace-insensitive diff), `sources.html` and `terms.html` both HTTP 200. §1.1/§1.2/§1.4/§1.6 and all of §2/§3 unaffected. Triggered the critical-issue entry above and the fix in the next row. |
| 2026-09-19 (7th cycle) | **Live page after the date fix** — commit `e4adf11`, deployed and re-fetched | PASS | PASS | PASS | **PASS** | Publish-then-verify, as GOVERNANCE.md contemplates where waiting is not tenable — leaving a knowingly false sentence live was judged the worse of the two errors. **§1.1**: every figure traces — 4.9 and the six chart values to EIA Global Energy Security Data Table 4; 1.5–6.9 to the `methodology.md` §4 back-test; `retrieved_utc` updated to 2026-09-19 because the source genuinely *was* re-fetched this cycle rather than carried over. **§1.2 (spot re-fetch, don't trust cache)**: fetched `eia.gov/outlooks/steo/report/energysecurity/article.php`, HTTP 200 — supplement release date still **"August 12, 2026"**, Table 4 total oil `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9`, identical to `hormuz.json`. No new EIA release, so no re-anchoring was due. The parent-STEO dates on the page ("September 9, 2026", "October 6, 2026") were seen and **deliberately not copied** — that trap has now been checked three cycles running. **§1.3**: "Friday 18 September 2026" — weekday verified programmatically against the date. **§1.4/§1.6**: "estimate" present in `<title>`, H1, unit line and standalone label; band 1.5–6.9 visible under the figure; methodology linked. **Horizon re-checked, not assumed**: 18 Sep is **80 days** past the 2026-06-30 anchor end, inside `MAX_HORIZON_DAYS = 92`, so persistence and the band both still sit inside their back-tested range and neither needed to move. **§2**: no prose touched at all this cycle, so nothing new to check for copying; chart remains an original rendering. **§3.4**: no copy went live — a date field inside the design the owner already approved on PR #4 is not new copy, and that reading is recorded in `decisions-log.md` so it can be challenged. Verified live after deploy: served `for_date: 2026-09-18`. |
| 2026-09-19 (7th cycle) | **GPCI / regime detector v3 / EIA STEO Table 3d** — new methodology work, `methodology.md` §13 (analysis + memory only, **nothing published, no figure changed**) | PASS | PASS | PASS | **PASS — and the PASS is the reason nothing shipped** | Run against the *analysis*, since §13 changes no live figure. **§1.1**: every number in §13 recomputed from the downloaded workbook in one script, not hand-carried — GPCI monthly series, the six flow/GPCI ratios, calm mean 1.076 / sd 0.006, 2026Q3 GPCI 14.42, and the three implied flows 5.9 / 11.8 / 15.5. **§1.2**: `STEO_m.xlsx` fetched fresh this cycle (HTTP 200, 1,096,855 bytes, September 2026 release). **§1.3 — the clause that did real work here**: the history/forecast boundary was taken from the workbook's own `Last Historical Month--- 202608` field, not inferred from column headers. STEO is a **forecast** product running to 2027; reading the columns naively would have fed EIA forecasts into the model as observations. September 2026 onward excluded. A second §1.3 catch: a first label match pulled UAE from Table 3b as `papr_tc` (petroleum *and other liquids*) and would have been summed with `copr_` crude series — an apples-and-oranges error producing a plausible-looking total that nothing downstream would have flagged. UAE and Qatar dropped instead; the incompleteness is recorded in §13.6 rather than hidden. **§1.4**: GPCI is labelled an index and a *pacing signal* throughout, never a flow measurement, with the storage/refining/bypass-pipeline caveats in the text. **§1.6 (inputs all independently cleared, never one used "just to calibrate")** — the clause under the most pressure this cycle, given the finding contradicts our published number: Table 3d's licence was read first-hand at `eia.gov/about/copyrights_reuse.php` (US federal public domain) before any use, and UKMTO and MARAD, both 403 at origin, were recorded **UNRESOLVED and dropped** rather than read around — no user-agent spoofing was attempted, precisely because these are sources whose access terms would matter most if they were ever ingested. **§3.1**: serves OKR KR6, the standing owner mandate. **§3.2** is why this shipped to an issue and not to the site: the evidence says our published 4.9 / 1.5–6.9 is probably low, so the honest move was critical issue #9 plus four options, not a quiet re-fit. **§3.4**: the copy describing the new input is held at `pending-copy/2026-09-19-sources-v3-gpci.md`, marked conditional and not to be banked if the owner declines — `sources.html` says what is in the model *today*, and GPCI is not in it. |
| 2026-09-19 (8th cycle) | **PR #8 re-verified immediately before merge** — branch `model/companion-chokepoints-and-daily-refresh` at `82d6e86` (the PR head `2d12780` plus this cycle's two plumbing fixes); pre-publish check | PASS | PASS | PASS | **PASS — merge authorised by explicit owner instruction** | This is a **re-run, not a re-use**, of the 2026-09-18 PR #8 row: the branch head had moved three times since that check (two merges of `main` on 18 and 19 Sep, plus this cycle's fix commit), and the owner's decision arrived a day later, so the earlier PASS was treated as stale. **§1.1/§1.2 (spot re-fetch, don't trust cache)** — re-fetched `eia.gov/outlooks/steo/report/energysecurity/article.php` today, 2026-09-19 ~17:11 UTC: the **supplement's own** release banner still reads **"August 12, 2026"** (the parent-STEO banner on the same page was again seen and again not copied — fourth cycle running this trap has been checked), Table 4 total oil `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9`, identical to `hormuz.json` and to what is live. No new EIA release, so no re-anchoring was due and **no published figure changes on merge**. **§1.3/§1.4/§1.6** — the script was executed against live EIA data in a scratch worktree and printed `estimate dated 2026-09-18, horizon 80 d, regime disrupted/local`, point **4.9**, band **1.5–6.9**: it reproduces the hand-built page exactly, which is the evidence that the automation is faithful rather than a silent re-derivation. Both non-nominal paths re-tested on the fixed code: horizon-exceeded renders the "No current estimate" block with `point/band = null`; a bad source URL exits **non-zero and writes nothing**. **Diff discipline**: the effective diff against `main` was inspected file by file and is still exactly the reviewed change set — no GPCI / critical-issue-#9 material leaked in from the 7th cycle's work, which is held in `pending-copy/` and not in this PR. **§2** — no prose changed by this cycle's fix commit; the only `sources.html` copy is the text self-checked on 2026-09-18 and put to the owner then, which the owner did not object to. **§3.1** — serves KR2's "auto-updating" half and KR6; no monetization content. **§3.2** — nothing in the change claims more authority: the horizon guard makes the model *refuse to answer* past its evidence, which is the opposite of overclaiming. **§3.3** — $0. **§3.4 (standing copy checkpoint)** — satisfied by the owner's explicit approval of the product question and their non-objection to the rest of the PR's copy after a day's visibility; recorded in `decisions-log.md` as owner-instructed, **not** as a CEO self-approval, because the distinction is the whole point of the checkpoint. **Two defects this check FOUND rather than waved through** (both fixed in `82d6e86` before merge, neither touching a figure, band, threshold or word of prose): (1) the bot's push would not have fired the Pages deploy, so the repo would have looked fresh while the live page froze — the automation would have failed silently at its only job; (2) the page write was not idempotent, adding a blank line per run so the "nothing changed, publish nothing" guard could never fire. Fixed output verified **byte-identical over three consecutive runs**. **One defect this check flags rather than fixes**: in the suppressed state the fixed sentence below the generated block still reads "The range is this wide because…", which will read incoherently from ~1 Oct. It is user-facing prose, so it is held at `pending-copy/2026-09-19-suppressed-state-note.md` for owner review rather than fixed in passing. |
| 2026-09-19 (8th cycle) | **The LIVE page after merge and after the first real run of the automation** — `https://oilthroughhormuz.com/` at `3ecd690` (post-publish verification) | PASS | PASS | PASS | **PASS** | Run against what is actually on the wire. **Availability/transport**: `index.html`, `sources.html` and `data/hormuz.json` all HTTP 200 with `ssl_verify_result=0`. **Content identity**: served HTML diffed whitespace-insensitively against `main` — **identical**, twice: once after the merge deploy (`35457474108`, commit `160498a`) and again after the automation's own deploy (`35457530338`, commit `3ecd690`). So what is published is what was reviewed, and the robot did not smuggle anything in. **§1.3 (the clause that has failed two days running)**: headline reads "Friday 18 September 2026", served on Saturday 19 September — correctly yesterday, and now correct *by mechanism* rather than by a human remembering. **§1.1/§1.2**: figures unchanged and matching today's source re-fetch; `retrieved_utc` is `2026-09-19T17:15:37Z`, written by the job itself, so the timestamp now means what it says. **§1.4/§1.6**: "estimate" in `<title>`, H1, unit line and label; band **1.5–6.9** visible under the figure; methodology linked. Horizon **80 of 92 days** — inside the back-tested range, so persistence and the band are both still supported. **§1.5**: `sources.html` now carries the "When we stop answering" section, so the reader is told in advance that the number will disappear rather than discovering it. **§2**: no prose changed by the automation, by construction — it writes only inside the `GENERATED:estimate` markers; verified the only delta in the bot's commit was the estimate block whitespace and two JSON fields. **§3.2**: unchanged; the page still says the figure is our model's estimate and not an official measurement. **§3.4**: no new copy reached live — every word on the live page is either owner-merged v3 text or the `sources.html` text carried by PR #8. **What this PASS does not cover, said plainly**: it is a check of a *manually dispatched* run. The unattended 05:10 UTC cron has not yet fired, and critical issue #9 — the standing argument that 4.9 / 1.5–6.9 is probably too low — is untouched by any of this and remains open with the owner. |
| 2026-09-20 (9th cycle) | **(1) the LIVE page as served, and (2) the bypass analysis** (`methodology.md` §15 + `scripts/bypass_analysis.py`). Nothing was published this cycle — check (2) is run against the *analysis*, following the 2026-09-19 precedent where doing so caught two errors that would have been invisible once published. | PASS (with one §1.3 observation) | PASS | PASS | **PASS** | **(1) Live page.** `https://oilthroughhormuz.com/` HTTP 200, `ssl_verify_result=0`; `data/hormuz.json` HTTP 200; served HTML **byte-identical** to `main` at `e2c812e`, so what is live is what was reviewed. **§1.1/§1.2 — source spot re-fetched, not trusted from cache**: EIA supplement release still **August 12, 2026**; Table 4 still `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9`; page figure 4.9 and band 1.5–6.9 both match. No new quarter published, so no re-anchor is due. **§1.3 — the one observation**: headline read "Friday 18 September 2026" at 02:20 UTC on 20 September, i.e. two days back rather than one. Assessed in full in `critical-issues-log.md` (2026-09-20) and deliberately **not** treated as a §1.3 FAIL: the refresh job computes `today(UTC) − 1` and runs at 05:10 UTC, so this is the known, bounded, self-correcting 00:00–05:10 window, not a frozen date. Recorded as an observation rather than silently passed, because the previous two §1.3 FAILs were this same field. The honest residual — that a single global date label is wrong for Gulf readers overnight — is held as copy at `pending-copy/2026-09-20-date-semantics.md`. **§1.4/§1.6**: "estimate" present in `<title>`, H1, unit line and label; band visible under the figure; methodology linked. Horizon 81 of 92 days — still inside the back-tested range. **§1.5**: `sources.html` "When we stop answering" still tells readers in advance that the number will disappear. **(2) The analysis.** **§1.1**: every input traces to a cleared source and the script **re-fetches both from source on every run** rather than reading numbers out of this repo — GPCI was independently rebuilt from `STEO_m.xlsx` and reproduces §13.2 to two decimals. **§1.6 (the clause that matters most here)**: no uncleared source was used, including "just to sanity-check" — in particular **no pipeline-capacity figure was used anywhere**, despite one being the obvious way to make the bypass attribution look more solid, because a capacity number doing work on a published figure is an input and would need a licence. The bypass term rests only on residuals from the already-cleared chokepoint table. **§1.4**: the Q3 projection is labelled throughout as a projection resting on an *assumed* Q3 bypass, and the script prints that caveat first. **A real check, not a rubber stamp — it changed the write-up twice**: the placebo control returned +0.58 on the Turkish Straits, so the method's noise floor is stated as ±0.58 rather than the flattering ±0.02/±0.10 from the other two controls; and §13.3's "2026 is a transit constraint" claim was corrected to "overstated by roughly a factor of two" rather than defended, since the bypass term explains 42% of the ratio collapse. **§2**: all prose original; no text lifted from EIA. **§3.1**: serves KR6, the standing owner mandate — and stayed inside read-only mode (the week-of-21 plan is still `PROPOSED`), touching nothing under `site/`. **§3.2**: no claim of authority is made anywhere; the finding is filed as evidence on critical issue #9 for the owner to decide, not applied to the live figure. **§3.3**: $0. **§3.4**: not applicable — nothing published; two copy drafts held. **What this PASS does not cover**: the first unattended cron run (05:10 UTC today, ~2h50m after this check) is unverified, and the analysis has had no specialist review, for the ninth consecutive cycle. |
| 2026-09-21 (10th cycle) | **`methodology.md` §16 + `scripts/weekly_arrivals_analysis.py`** — KR6 cycle 5: a weekly cleared input (EIA Weekly Preliminary Crude Oil Imports by Country of Origin) and the Saudi/Iraq split. **Analysis only; nothing under `site/` was touched.** | PASS | PASS | PASS | **PASS** | **§1.1/§1.2**: every figure re-derives from source on each run — the script re-fetches all nine country series and the landing page, and records EIA's own release date (2026-09-16) and next-release date (2026-09-23). Nothing is transcribed by hand. **§1.3 caught a real error again, which is the third cycle running it has earned its place**: the draft described the series as "~0.3–0.6 m b/d, a 2–4% sample", but March 2026 reached **0.87** and July reached **0.00** — the stated range was simply wrong at both ends. Corrected in both the document and the script docstring to the 2025 mean (0.46 m b/d, ~3%) with the 2026 swing stated separately. Units are held apart deliberately throughout: this series is thousand b/d, the Hormuz anchor is million b/d, and the §16.5 comparison is made in **percentages** so the two are never summed — the same class of unit trap §1.3 caught on 2026-09-19. **§1.4**: the voyage lag is labelled an assumption (35–55 days) and shown as a range, never fitted and never presented as measured; the Feb–Mar run-up is filed explicitly as "a hypothesis, not a finding". **§1.6**: licence re-read **first-hand this cycle** at `eia.gov/about/copyrights_reuse.php` rather than inherited from prior cycles, including its "protected materials" carve-out, which was checked and found not to bite (EIA/Census survey statistics, not a vendor feed). `ir.eia.gov` returned **403** on one probe and was left alone — not retried with a spoofed user-agent, consistent with the UKMTO/MARAD precedent. No uncleared source was used anywhere, including to sanity-check. **A real check, not a rubber stamp — it changed the conclusion, not just the wording**: the per-origin split (Saudi **+16.4%**, Iraq **−89.0%**) was added *because* §1.4 asked what the aggregate −27.3% was hiding, and it reversed the recommendation from last cycle's "C′ after review" to "**C′ not before a 3Q26 chokepoint observation**". The new input argues against this company's own preferred reading of issue #9 and that is written up as the headline of §16.4 rather than buried. **§2**: all prose original; the one EIA licence sentence is in quotation marks with attribution. **§3.1**: serves the KR6 standing mandate and the week-of-09-21 plan's named lead ("does EIA publish anything *weekly* that is Gulf-relevant?"). The plan is still `PROPOSED`, so this stayed strictly inside read-only prep — no spend, no publish, no hard-to-reverse action. **§3.2**: `sources.html` deliberately **not** updated. The input is analysis-only, and listing an analysed-but-unused source would overstate the model's input diversity — category 4, and the same reason §13's GPCI copy is still held and conditional. **§3.3**: $0. **§3.4**: not applicable — nothing published; no new copy drafted, and that was a deliberate call (two one-line drafts are already queued unreviewed; adding a third speculative one for an input the owner has not accepted would be noise, not diligence). **What this PASS does not cover**: no specialist review, tenth consecutive cycle; and the Saudi-vs-bypass ambiguity in §16.4 is genuinely unresolved with cleared data until EIA publishes 3Q26 chokepoint volumes (~November). |
| 2026-09-22 (11th cycle) | **(a) The live page as served** (no change made to it), and **(b) the KR6 §17 analysis** — Eurostat EU crude-import corroboration, `scripts/eu_imports_analysis.py`. **Nothing published this cycle**; the week-of-21 plan is still `PROPOSED`, so this is a run against the live state plus the analysis, not a pre-publish gate. | PASS | PASS | PASS | **PASS — and §1.3 caught a third-party freshness claim before it became a published one** | **Why run the rubric at all on a read-only cycle**: because §1.3 has now caught a real error on four consecutive cycles, and every one of them was in *analysis* rather than in page copy — i.e. in the material that later becomes a number. **§1.1/§1.2 (traceable, spot re-fetch)**: EIA supplement re-fetched this cycle, HTTP 200, release still **2026-08-12**, Table 4 total-oil row `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9` — identical to `site/data/hormuz.json` and to every figure rendered. Live bytes byte-identical to `origin/main` (`821eb9f`). **§1.3 (units, dates, time zones) — the catch.** Eurostat's dataflow metadata advertises `OBS_PERIOD_OVERALL_LATEST = 2026-07`, and the draft of §17 initially recorded that the 2026-07 gap was a *Gulf-partner* gap. Direct check of the actual cells showed 2026-07 is **null for every partner tested — Saudi, US and Norway alike** — so the crude-oil-by-partner slice simply ends at 2026-06 and the 2026-07 edge belongs to a different slice. Corrected in place before the section was finished. This matters more than it looks: had it stood, the company would have recorded a new input as *one month fresher than the anchor* when it is exactly as stale, and a freshness claim is the sort of thing that later gets repeated to a reader. **§1.3, second application**: the new series is **thousand tonnes per month** and the anchor is **million barrels per day**; the script compares only percentages against each partner's own calm mean and never sums or rescales across the two, carrying forward the §16 discipline. **§1.4 (estimates labelled as estimates)**: unchanged — the live headline still carries "Our model's estimate", the band, and the not-a-measurement framing. **§1.5 (gaps surfaced)**: no new gap reached the reader; two known ones remain *held* rather than hidden — the suppressed-state sentence (`pending-copy/2026-09-19-...`) and the estimate-date time zone (`pending-copy/2026-09-20-...`), both awaiting owner review. Scheduler latency is now measured at ~4.6–5.5h late across three observations, so the honest worst-case stale window is ~10–11h rather than the ~5h previously assumed; that belongs in the date-semantics draft and has been noted there in `critical-issues-log.md` rather than published unilaterally. **§2 (no copied text)**: no prose was written for publication this cycle. The Eurostat licence terms quoted in §17 and in the critical-issues log are in quotation marks with the source URL, as required. **§3.1 (on-purpose)**: the week-of-21 plan names "bucket (2) event/advisory signals" plus input expansion as Research work; the Agent tool is unavailable for the **11th** consecutive cycle (`No such tool available: Task`), so the CEO ran the reduced-depth pass the plan explicitly authorises for that case. UKMTO, MARAD MSCI and OPEC were re-probed and all returned **403 at origin**; none was worked around, none is recorded as licence-rejected. **§3.2 (does not misrepresent authority) — the clause under real pressure this cycle, and it held.** A genuinely good new input was found and **not adopted**, because its licence is only half-clear (Eurostat non-EU partner data is non-commercial-only by an exception whose scope is ambiguous). `sources.html` was **not** updated to mention it: listing an analysed-but-unused source would overstate the model's input diversity, which is category 4. §16's claim that "three structurally unrelated inputs now agree" was also **corrected** in §17 — they were three different series from *one publisher*, and overstating independence is the same failure mode in a quieter register. **§3.3 (spend)**: $0. **§3.4 (copy checkpoint)**: nothing reached live `site/` content; the two pending drafts remain held. |
| 2026-09-23 (12th cycle) | **No publish this cycle.** Rubric run against (a) the live page, as a standing check, and (b) the new analysis-only input (`methodology.md` §18, `scripts/japan_imports_analysis.py`) before it could become a claim anywhere | **PASS (live page); analysis corrected before it was recorded** | PASS | PASS | **PASS, no publish** | **Live page (§1.1/§1.2)**: bytes identical to `origin/main` `0622a81`; EIA Table 4 re-fetched and unchanged; `for_date` 2026-09-21 inside the design window. **§1.3 (units, dates) caught two things in our own analysis before they were written up.** (1) The calm-mean denominator used 730 days, but 2024 is a leap year and Feb-2024 volume was in the numerator. Fixed to 731; results moved by ≤0.1 pt, but a units/dates slip is exactly what this item exists for. (2) Cargo granularity: Kuwait's "−100% → −55% recovery" is roughly **zero VLCC cargoes to one** (calm ≈ 2.3 cargoes/month). The write-up says so explicitly and forbids scaling it into a level. **§1.4/§1.6 (estimates labelled; inputs cleared; no "just calibrating")**: the new input's licence was read first-hand (three documents: Customs notice EN+JA, PDL1.0, e-Stat terms) **before** any data was downloaded. It moved no published number. **An overclaim was caught and removed**: the first draft output reported a "Gulf-vs-control divergence of −716 points", but for Japan the non-Gulf group is the *substitution* source, not an independent placebo. Reporting that number would have manufactured confidence from a mechanical artefact. Replaced with a total-imports demand check. **§2 (no copied text)**: licence text quoted in quotation marks with URLs; no prose drafted for publication. **§3.1 (on-purpose)**: KR6 standing mandate plus the plan's input-scouting item; reduced-depth CEO pass because the Agent tool is unavailable (12th cycle). **Access discipline re-checked**: early probes sent a browser user-agent. Verified afterwards that e-Stat and Customs serve default clients (curl and Python), so no refusal was bypassed. The script now sends an honest project UA. METI refused all clients (403) and was left alone. **§3.2 (authority)**: `sources.html` **not** updated; PDL1.0's "do not present edited content as government-produced" condition is recorded for any future copy. **§3.3**: $0. **§3.4**: nothing reached `site/`; pending drafts still held. |
| 2026-09-23 (owner response) | **PR #11 — option-C re-anchor** (branch `model/option-c-gpci-reanchor`, commit `d6fbdab`): `scripts/refresh_estimate.py` rewritten, new `scripts/gpci.py`, `site/index.html` (generated block + one clause), `site/sources.html` (three sections), `site/data/hormuz.json`, refresh workflow commit text. **Pre-publish, NOT live.** | PASS | PASS | PASS | **PASS — publish gated on owner approval of PR #11** | **§1.1**: mechanical number scan of rendered text. `index.html` → axis {0,5,10,15,20}, quarter labels {25,26}, series {20.9,21.0,21.3,21.6,14.9,4.9}, point **5.6**, band **4.9 / 10.8**, {3%}, date {22, 2026}. `sources.html` → {2%, 45%, 51%, 84, 94%, 204%} (back-test, `hormuz.json` → `model.estimator.backtest`), {3%} calm, {10%} regime, {2, 3, 4} (table numbers), {12} (release date), {2026}. No orphan numbers. Point verified by hand: 4.9 / 11.953 × 13.57 = 5.563; high: × 1.945 = 10.82. **§1.2**: EIA supplement re-fetched this cycle (release 2026-08-12, Table 4 unchanged); `STEO_m.xlsx` re-fetched (September 2026, Last Historical Month 202608). **§1.3**: 2026-09-22 verified as a Tuesday. "two months past the end of the anchor" checked (Aug vs Jun). **This check caught one real issue**: EIA asks that acknowledgements include the publication date, and the new STEO citation lacked it. Added "(September 2026 edition)" before commit. **Earlier in the same session, an overclaim was caught before it reached any copy**: "beats persistence in every quarter" holds only for a hindsight variant. The page states the realistic figures, including that the method is no better than persistence at the 1Q26 onset. **§1.4**: "estimate" still in `<title>`, H1 and label; unchanged. **§1.5**: single-publisher dependence stated on the page. The transit-share assumption is named as the one most likely to be wrong. Production-is-not-transit caveat is visible. AIS caveat retained. **§1.6**: method published (`sources.html` + `methodology.md` §19). Inputs are all cleared: EIA reuse terms re-read first-hand this cycle, HTTP 200, public-domain grant unchanged. The analysis-only series (EIA weekly, Eurostat, Japan) are not inputs to the formula. Visible range retained. **§2**: all prose original; no script/img/iframe on either page. **§3.1**: authorised specifically by the owner's issue #9 comment. Plan #10 is still PROPOSED, and this is not treated as approving it. **§3.2**: "two cleared datasets, one publisher". The analysis-only inputs are **not** listed. Nothing implies we observe the strait. The generated sentence "headline as its midpoint" was replaced because it would have become false. **§3.3**: $0. **§3.4**: held in PR #11, `needs-copy-review`, not self-merged. **Edge paths (scratch copy)**: 1 Oct suppression; 3Q26 arriving while suppressed (re-anchors, scores last published vs persistence); rolled table (labels correct); falling production (mirrored band and sentence); stale GPCI (refused); broken page (refused, nothing written); extra value in a row (refused); idempotent page write. **Not covered**: no specialist review (13th cycle); k's symmetry in recovery is unverified until 3Q26. |
| 2026-09-23 (owner governance session) | **PR #11 re-checked immediately before merge, then the LIVE page after merge** (merge `2d98638`, deploy `35852729377`). The first publish under the CEO's new publishing authority, with no owner pre-approval, so the full rubric is the gate. | PASS | PASS | PASS | **PASS, published** | **Re-run, not re-used:** the morning PASS (row above) was treated as stale. **§1.1/§1.2:** the branch's refresh job was re-run against live sources in a scratch worktree with `main` merged in. It re-fetched the EIA supplement (Table 4 unchanged, release 2026-08-12) and `STEO_m.xlsx` (Sept 2026, last historical month 202608), and reproduced **5.6, band 4.9–10.8, k=1.945, GPCI 13.57 vs anchor 11.95**. The only diff was `retrieved_utc`. **Live:** `index.html`, `sources.html` and `data/hormuz.json` HTTP 200, all **byte-identical** to `main`; the served figure is 5.6 with range 4.9–10.8. (TLS through the re-signing proxy; origin cert per the 12th-cycle CT check.) **§1.4/§1.6:** "estimate" in title, H1 and label; range visible under the figure; method linked; inputs are two cleared EIA datasets. **§2:** zero script/img/iframe on both pages. **§3.1:** serves G1 (accuracy) and the owner's option-C decision on #9. **§3.2, including the new headline test:** read as a lone line, the label says "Our model's estimate…", and the figure never appears without its range; `sources.html` says "two cleared datasets, one publisher"; nothing implies official or real-time data. **§3.3:** $0. **§3.4 (new form):** via PR, relabelled `ceo-published`, with a what-and-why comment posted before merge. **Doubts recorded, not blocking:** the headline shows only ~7 days before the unchanged horizon blanks it; the band's k was measured only on the way down. |
| 2026-09-23 (owner governance session) | **PR #12**: suppressed-state note and UTC date label (branch `site/suppressed-note-and-utc-label`, merge `16def17`, deploy `35852947613`). Pre-merge check plus live verification. | PASS | PASS | PASS | **PASS, published** | Copy drafted 2026-09-19/20 (`pending-copy/`), held for owner review ever since, published now under publishing authority. **No figure, band or chart changed.** The refresh run re-fetched both sources and reproduced 5.6 / 4.9–10.8. **§1.3, the clause this change exists for:** the label now says "(UTC)", and the methodology clause says "most recent completed day (UTC)" instead of promising "the day you are reading", which was false for Gulf readers overnight. 2026-09-22 is a Tuesday, and it is the most recent completed UTC day at check time (09-23). **§1.5:** in the suppressed state, the sentence under the block now reads "This is deliberate, not a fault" instead of "The range is this wide", verified by a simulated 2026-10-02 run. The calm-state variant drops "this wide", which would be false at ±3%. **Mechanics:** a second run is byte-identical (idempotent); the script fails loudly if either generated region is missing. **§2:** original prose. **§3.2 headline test:** the label reads as a dated estimate, not a measurement. **§3.4:** PR `ceo-published` with what-and-why. **Live:** HTTP 200, `index.html` and `hormuz.json` byte-identical to `main`, label served with "(UTC)". **Residual found, not fixed in passing:** in the suppressed state the methodology paragraph still says "Right now that range is wide". It is below the fold and not false in context, but it is stale. Backlog. |
