# Self-Check Log

Append-only. One row per rubric run (see [rubric.md](rubric.md)). A `FAIL` on
any column means overall `FAIL` — log it anyway, don't skip logging failures.

| Date | Item checked | Accuracy | No copied text | On-purpose | Overall | Notes / evidence |
|---|---|---|---|---|---|---|
| 2026-09-17 | Draft homepage copy, `company-memory/pending-copy/2026-09-17-homepage.md` (pre-publish check; NOT live) | PASS | PASS | PASS | PASS (as draft — publish still blocked) | **Accuracy**: draft contains zero figures by design; every live-number slot is a `[FIGURE: …]` placeholder, so there is nothing to trace or spot-re-fetch. Draft B builds source + coverage period + retrieval timestamp into the figure block, and Draft C commits to surfacing staleness and to naming AIS jamming/spoofing/dark-vessel uncertainty — satisfying rubric 1.4 (estimates labelled as estimates) and 1.5 (gaps surfaced). **No copied text**: all prose written by the CEO this cycle; no quoted material, so no attribution needed; no third-party charts or images. **On-purpose**: the week-of-2026-09-14 approved plan names "Draft homepage copy (headline + short methodology/about blurb) and hold it for owner review" as a CEO item. No monetization content. Drafts C/D/E explicitly disclaim official authority and state non-affiliation (rubric 3.2). $0 to produce (3.3). Rubric 3.4 (owner review before live) is **satisfied as "held, not yet approved"** — sent to owner as GitHub issue #2, label `needs-copy-review`; it must not be published until that issue is approved, and this row must not be read as clearance to publish. |
| 2026-09-17 (2nd cycle) | Live site, **actually fetched this time** — `https://wp-tszabo.github.io/oil-through-hormuz/` and `http://oilthroughhormuz.com/` | PASS | PASS | PASS | **PASS** (supersedes the INCONCLUSIVE row below) | Egress restored, so the spot-check the rubric asks for was finally performable. **Accuracy**: fetched both live URLs and diffed against the repo's `site/index.html` — byte-identical ignoring whitespace, so what is deployed is what is reviewed. Scanned the served HTML for numeric content: the only digits present are CSS values (`0.2rem`, `640`, `999px`, `#16181a`, etc.). **Zero data figures are live**, so there is no number to trace, no unit/timezone ambiguity, no unlabelled estimate, and no staleness to surface — rubric §1.1–1.5 pass vacuously but genuinely. **No copied text**: prose is the CEO's original placeholder; no quotes, no third-party charts or images. **On-purpose**: serves KR2 (site skeleton live); page self-labels "Phase 1 — under construction", claims no authority, links the public governance repo; no monetization content; $0 to produce. §3.4 satisfied — nothing on the live page is copy pending review; the drafts in `pending-copy/` are still held. **Caveat recorded, not hidden**: this PASS covers *content correctness*, not availability. The site is simultaneously **degraded** — HTTPS on the custom domain fails TLS (critical issue #3, GitHub issue #3). The rubric has no availability column, so that is tracked as a critical issue rather than silently folded into this PASS. |
| 2026-09-17 | Live site (`site/index.html` as deployed) — attempted verification | NOT VERIFIABLE | PASS | PASS | INCONCLUSIVE — logged, not treated as a pass | Could not fetch `oilthroughhormuz.com`, `www.oilthroughhormuz.com` or `wp-tszabo.github.io`: egress policy denial (see critical issue #1). Indirect evidence the site is up: apex DNS resolves to 185.199.108–111.153 (GitHub Pages), `www` CNAMEs to `wp-tszabo.github.io`, last Pages deploy (`3fd9200`) succeeded 2026-09-16T20:11Z. Reviewed the repo copy of `site/index.html` instead: contains no figures, labels itself "Phase 1 — under construction", claims no authority — so nothing currently live can be inaccurate about data. Recorded as INCONCLUSIVE rather than PASS because inference from DNS is not the spot-check the rubric asks for. |
