# Critical Issues Log

Append-only. Log every critical issue the moment it's raised — even if it's
resolved within the same cycle. See GOVERNANCE.md for the four categories
(outage/stale data, overspend, legal/compliance exposure, hard-to-reverse
action) and the escalation mechanism (GitHub issue, label `critical`).

| Date raised | Category | Description | Immediate action taken | Status | Resolved date |
|---|---|---|---|---|---|
| 2026-09-17 (3rd cycle) | 4 (hard-to-reverse / misrepresenting the data's authority) — **pre-emptive, action not taken** | The owner's page spec (issue #2 comment) asks for a headline reading *"The amount of oil passed through the Strait of Hormuz on {yesterday's date}"* followed by a number. Executed literally, that puts a figure under a date and tells the reader it was measured that day. **No source this company may legally republish measures Hormuz flow daily** — verified this cycle: IMF PortWatch (daily) rejected on licence last cycle; the **WTO Strait of Hormuz Trade Tracker** (`datalab.wto.org`, daily) found and **rejected this cycle** because the underlying data is AXSMarine (Signal Group), a commercial vendor, published under "© WTO 2026" with no open-data grant; EIA (cleared) is a quarterly average ~6 weeks in arrears. Any daily figure is therefore a model output extrapolated from a quarterly anchor, current honest band ≈ **1.5–7 m b/d** — wider than the quantity estimated — around an anchor EIA itself says is revised frequently because Hormuz AIS data has been unreliable since end-February 2026. Presenting that as yesterday's measured flow is rubric §1.4/§3.2 FAIL and category 4. | **Did not publish it, and did not quietly design around it.** The draft page headlines the latest *measured* quarter (4.9 m b/d, April–June 2026) with its coverage period stated, plus an explicit note that this is not yesterday's figure and why. Nothing merged to `main`; the Pages workflow is `main`-only so the branch cannot reach the live site. Model, back-test and reasoning written up in `company-memory/methodology.md` so the judgement is reviewable rather than asserted. Raised as GitHub issue **#5** (`critical`), with the decision itself put to the owner in **PR #4** (three options, including an honest daily *range* version that stays closer to the requested layout). | OPEN — awaiting owner decision in PR #4 | — |
| 2026-09-17 (3rd cycle) | 4 (hard-to-reverse — near-miss loss of company memory) | **An entire cycle of company memory existed only on an unpushed, unnamed local commit and was one `git checkout` away from being destroyed.** Discovered mid-cycle: the working tree was on a **detached HEAD** at `2b30fb6`, while `main` and `origin/main` were both still at `3d10b68` — the *first* cycle's commit. The second cycle's three commits (`46848fd` PortWatch rejection + broken-HTTPS critical issue, `9dea829` Research mandate extension, `2b30fb6` CEO agent update) had been committed but **never pushed and never merged to any branch**. Consequences if lost: the PortWatch licence rejection, the HTTPS critical-issue record, the owner's estimation-methodology directive and the updated rubric §1.6 would all have vanished, and a future cycle doing a fresh clone would have silently re-run research already done and possibly re-approached a source already rejected on legal grounds. GOVERNANCE.md names data deletion as a hard-to-reverse action; losing the decisions log is precisely that, and the company's own constitution says memory is the source of truth over any agent's recollection. | **Recovered and secured.** The commits were rescued incidentally — creating this cycle's `copy-review/homepage-v2` branch off the detached HEAD gave them a ref, and pushing that branch put them on the remote. Then verified the three commits touch **no** `site/` files (so no deploy risk) and fast-forwarded `main` to `2b30fb6`, putting two cycles of memory back on the canonical branch, and pushed. Cycle-3 work was then applied on top. **Root cause is procedural, not technical**: previous cycles committed memory but never verified the commit landed on `main` *and* on `origin`. Added to the backlog as a standing end-of-cycle check. | **RESOLVED 2026-09-17 (3rd cycle)** — `main` and `origin/main` now contain all three cycles. Logged rather than quietly fixed, because a near-miss on the company's own memory is exactly the class of failure this log exists for. | 2026-09-17 |
| 2026-09-17 | 3 (legal/compliance) + 1 (data verification) | Sandbox egress proxy returns a hard policy denial for every data-source and live-site domain the CEO needs: `eia.gov`, `api.eia.gov`, `imf.org`, `portwatch.imf.org`, `oilthroughhormuz.com`, `wp-tszabo.github.io`, and the GitHub Pages API path. Consequence: (a) no data source's ToS can be read in full, so none can be cleared — search snippets indicate IMF PortWatch is under the IMF's bespoke Data Terms (2024-10-11), explicitly not Creative Commons; (b) rubric §1's spot re-fetch is impossible, so the CEO cannot verify a published figure or detect a stale feed. No confirmed outage: DNS for the apex resolves to the four GitHub Pages IPs, `www` CNAMEs to `wp-tszabo.github.io`, and the last Pages deploy (`3fd9200`) succeeded 2026-09-16. | Paused all data wiring; cleared no source; published nothing (no commit touches `site/`, so no deploy fired). Candidate sources recorded as UNVERIFIED in `decisions-log.md`. Continued only the in-bounds part of the approved plan (copy drafting, held for review). Opened GitHub issue #1, label `critical`, with three options for the owner (allowlist / CI-side fetch+verify / accept the pause). | **MOSTLY RESOLVED 2026-09-17 (2nd cycle)** — egress restored without owner action; see the two rows below, which supersede this one. Issue #1 left open for the residual asks (CI-side verification; product re-scope decision). | 2026-09-17 (egress condition only) |
| 2026-09-17 (2nd cycle) | 3 (legal/compliance) — **resolved, negative** | Egress restored (all of `eia.gov`, `portwatch.imf.org`, `imf.org`, `wp-tszabo.github.io` returned HTTP 200 on re-test), so the ToS read blocked above was finally possible. **Result: IMF PortWatch is NOT redistributable for this product.** Every PortWatch dataset carries `license: custom` with `licenseInfo: https://www.imf.org/external/terms.htm` (verified first-hand via the ArcGIS Hub item metadata, not from search snippets). Those IMF terms permit only "free non-systematic downloading … for personal, noncommercial usage only without any right to resell, redistribute, compile, or create derivative works", and require permission "to copy or download IMF Content in any systematic way". An automated daily fetch → store → re-publish is squarely inside all four prohibited verbs and outside the narrow 1,000-word Fair Use carve-out. EIA, by contrast, verified clear: US government work, public domain, free to use and distribute with acknowledgement — but its Hormuz chokepoint release is annual/half-yearly and its freshest published figure (20.9 m b/d, 1H25) covers a period that ended ~14 months ago, while Q1-2026 flows are reported ~30% lower amid regional conflict. | **Nothing ingested, nothing wired, nothing published** — the guilty-until-checked rule caught this before any code depended on it. PortWatch recorded as REJECTED in `decisions-log.md`. Posted the full verdict plus three re-scope options to issue #1 (A: ask `copyright@imf.org` for written permission, $0; B: re-scope honestly around EIA's real vintage; C: Phase 2 paid-data conversation — explicitly **not** recommended until A is tried). **No email sent to the IMF** — outward-facing contact on the company's behalf is the owner's to authorise. | OPEN — awaiting owner decision on re-scope (issue #1) | — |
| 2026-09-17 (2nd cycle) | 1 (site not serving correctly) | **HTTPS is broken on the custom domain.** `https://oilthroughhormuz.com/` fails TLS: the edge presents `CN=*.github.io` and `subjectAltName does not match oilthroughhormuz.com`. `https://www.oilthroughhormuz.com/` fails the same way, and `http://www.…` 301-redirects *into* the broken HTTPS URL. Only `http://` on the apex (HTTP 200) and `https://wp-tszabo.github.io/oil-through-hormuz/` (HTTP 200) serve correctly. Cause: Pages has the custom domain configured but the Let's Encrypt certificate was never provisioned — the classic symptom of DNS landing after the domain was saved. **This was previously sitting in `backlog.md` as a routine "turn on HTTPS enforcement" task; it is not routine, it is the primary domain being broken for every normal visitor, so it is re-raised at critical severity.** Note the earlier egress block had been masking a genuine outage: "can't fetch our own site" was read as a proxy rule, and was in fact partly a real defect. | Opened GitHub issue #3, label `critical`. **Did not touch DNS or `site/CNAME`** — domain changes are a named hard-to-reverse action under GOVERNANCE.md. The Pages REST API path is blocked by the sandbox proxy (`Access to this GitHub API path is not permitted through this proxy`), so the CEO can neither read cert state nor toggle Enforce HTTPS; this needs the owner in **Settings → Pages** (clear + re-save the custom domain to re-trigger issuance, then tick Enforce HTTPS). `metrics.md` uptime recorded as **DEGRADED**, not up. | **RESOLVED 2026-09-17 (3rd cycle)** — owner cleared and re-added the custom domain in Settings → Pages, which unstuck certificate issuance. **Verified by the CEO before closing, not accepted on report**: `https://oilthroughhormuz.com/` now presents `CN=oilthroughhormuz.com` issued by Let's Encrypt (expires 2026-12-16), `SSL certificate verify ok`, HTTP 200 — and all four entry routes (`http://` apex, `https://www`, `http://www`, the `github.io` URL) now terminate on the valid-TLS apex, with HTTPS enforcement on (plain HTTP 301s up rather than serving). GitHub issue #3 closed with that evidence. `metrics.md` uptime moves DEGRADED → UP. **Lesson carried forward rather than just closed**: nothing in this company would have detected this on its own — it was caught by hand, and only because egress happened to be working that hour. The CI-side monitor must assert *certificate validity*, not just HTTP 200; an HTTP-only check would have reported green for the whole outage. | 2026-09-17 |
| 2026-09-17 (4th cycle) | 4 (hard-to-reverse — misrepresenting the data's authority) — **RESOLUTION of the pre-emptive issue #5 logged above** | Resolution row, not a new issue. The owner answered the escalation on PR #4 (comment `5716791659`, 15:16:58Z): *"The daily figure is the estimate. That is the real value of the whole page thta we develop a model that make that estimate. So let's add 'estimated ' keyword to the title but let's keep the daily figure."* That overrules the CEO's recommendation to headline the measured quarter, and it is exactly the decision the escalation asked for. | Implemented on `copy-review/homepage-v2`, **not merged**: headline is now the model's point estimate for 2026-09-16 (4.9 m b/d), with "estimate" in the `<title>`, the H1, the unit line and a standalone "Estimate — not a measurement" label, and the **1.5–6.9 m b/d band shown directly under the figure**. The band was not part of the owner's instruction; `rubric.md` §1.6 requires a visible range for any modelled figure and the owner was not asked to waive it, so both ship together — see `decisions-log.md` for that interpretation stated plainly enough to be challenged. Rubric re-run: **PASS** (`self-check-log.md`, 4th-cycle row), including a fresh EIA spot re-fetch and an explicit §3.2 re-read of the Terms and Copyright paragraphs, which had called the figures "third-party estimates" and stopped being true once the headline became our own model output. | **RESOLVED — the category-4 exposure is closed** (the page no longer asserts a measurement it cannot support). Publication remains gated: the owner resolved the content question without saying "merge", so the branch is updated and final merge approval is requested on PR #4. **One new, lower-severity risk created by the decision and recorded rather than absorbed**: the estimate date is hard-coded, so the page goes stale by construction — not a category-1 issue on publication day, but it becomes one if nothing re-dates it. Now the top Build item. | 2026-09-17 |
| 2026-09-17 (5th cycle) | 4 (hard-to-reverse / misrepresenting the data's authority) — **assessed, resolved in design, NOT raised as an interrupt** | Logged for the record because the assessment was real, not because an interrupt fired. The owner directed that the visible trend/chart copy not name an exact source, on the grounds that the product is a proprietary aggregation method rather than a pass-through of one provider. The company today has **exactly one** licence-clear numeric input. A literal maximal reading of that directive — remove the attribution and let "our proprietary model" stand alone — would imply either that we observe the strait ourselves or that we already blend many feeds. Both are false, and publishing either would be category 4 and a rubric §1.1/§3.2 FAIL. So the directive was treated as something to resolve carefully rather than execute literally. | **Resolved in design, nothing published.** The visible primary copy speaks only of "our model"/"our method"; the full attribution (publisher, dataset, table, release date, licence, Vortexa provenance disclaimer, the publisher's own AIS-reliability caveat, and the daily trackers checked and rejected on licence) moved **intact** to a new linked `sources.html`, reachable from the chart caption, the methodology blurb and the footer. `index.html` grep-verified to contain zero occurrences of EIA / Energy Information Administration / Vortexa / PortWatch / AXSMarine / WTO; `sources.html` carries all of them. No extra input diversity is claimed anywhere: `sources.html` states "One cleared dataset, and we say so rather than implying a wider net", and the aggregation ambition appears only as a "Where this is going" section describing ongoing work. Rubric re-run in full: **PASS**. Held on `copy-review/homepage-v3` and put to the owner as **PR #6**, with the reasoning written out on the PR so the owner can overrule it. | **RESOLVED IN DESIGN — no interrupt raised, and one stated in advance if the position changes.** The threshold is explicit: if the owner asks for *no* visible pointer to the source at all, the CEO will raise that as a category-4 critical issue **before** implementing it, rather than complying against its own judgement. That version crosses into misrepresenting the data's authority; the version shipped does not. Recorded here so the line is on the record before the conversation rather than after it. | 2026-09-17 |
