# Critical Issues Log

Append-only. Log every critical issue the moment it's raised — even if it's
resolved within the same cycle. See GOVERNANCE.md for the four categories
(outage/stale data, overspend, legal/compliance exposure, hard-to-reverse
action) and the escalation mechanism (GitHub issue, label `critical`).

| Date raised | Category | Description | Immediate action taken | Status | Resolved date |
|---|---|---|---|---|---|
| 2026-09-17 (3rd cycle) | 4 (hard-to-reverse / misrepresenting the data's authority) — **pre-emptive, action not taken** | The owner's page spec (issue #2 comment) asks for a headline reading *"The amount of oil passed through the Strait of Hormuz on {yesterday's date}"* followed by a number. Executed literally, that puts a figure under a date and tells the reader it was measured that day. **No source this company may legally republish measures Hormuz flow daily** — verified this cycle: IMF PortWatch (daily) rejected on licence last cycle; the **WTO Strait of Hormuz Trade Tracker** (`datalab.wto.org`, daily) found and **rejected this cycle** because the underlying data is AXSMarine (Signal Group), a commercial vendor, published under "© WTO 2026" with no open-data grant; EIA (cleared) is a quarterly average ~6 weeks in arrears. Any daily figure is therefore a model output extrapolated from a quarterly anchor, current honest band ≈ **1.5–7 m b/d** — wider than the quantity estimated — around an anchor EIA itself says is revised frequently because Hormuz AIS data has been unreliable since end-February 2026. Presenting that as yesterday's measured flow is rubric §1.4/§3.2 FAIL and category 4. | **Did not publish it, and did not quietly design around it.** The draft page headlines the latest *measured* quarter (4.9 m b/d, April–June 2026) with its coverage period stated, plus an explicit note that this is not yesterday's figure and why. Nothing merged to `main`; the Pages workflow is `main`-only so the branch cannot reach the live site. Model, back-test and reasoning written up in `company-memory/methodology.md` so the judgement is reviewable rather than asserted. Raised as GitHub issue **#5** (`critical`), with the decision itself put to the owner in **PR #4** (three options, including an honest daily *range* version that stays closer to the requested layout). | OPEN — awaiting owner decision in PR #4 | — |
| 2026-09-17 (3rd cycle) | 4 (hard-to-reverse — near-miss loss of company memory) | **An entire cycle of company memory existed only on an unpushed, unnamed local commit and was one `git checkout` away from being destroyed.** Discovered mid-cycle: the working tree was on a **detached HEAD** at `2b30fb6`, while `main` and `origin/main` were both still at `3d10b68` — the *first* cycle's commit. The second cycle's three commits (`46848fd` PortWatch rejection + broken-HTTPS critical issue, `9dea829` Research mandate extension, `2b30fb6` CEO agent update) had been committed but **never pushed and never merged to any branch**. Consequences if lost: the PortWatch licence rejection, the HTTPS critical-issue record, the owner's estimation-methodology directive and the updated rubric §1.6 would all have vanished, and a future cycle doing a fresh clone would have silently re-run research already done and possibly re-approached a source already rejected on legal grounds. GOVERNANCE.md names data deletion as a hard-to-reverse action; losing the decisions log is precisely that, and the company's own constitution says memory is the source of truth over any agent's recollection. | **Recovered and secured.** The commits were rescued incidentally — creating this cycle's `copy-review/homepage-v2` branch off the detached HEAD gave them a ref, and pushing that branch put them on the remote. Then verified the three commits touch **no** `site/` files (so no deploy risk) and fast-forwarded `main` to `2b30fb6`, putting two cycles of memory back on the canonical branch, and pushed. Cycle-3 work was then applied on top. **Root cause is procedural, not technical**: previous cycles committed memory but never verified the commit landed on `main` *and* on `origin`. Added to the backlog as a standing end-of-cycle check. | **RESOLVED 2026-09-17 (3rd cycle)** — `main` and `origin/main` now contain all three cycles. Logged rather than quietly fixed, because a near-miss on the company's own memory is exactly the class of failure this log exists for. | 2026-09-17 |
| 2026-09-17 | 3 (legal/compliance) + 1 (data verification) | Sandbox egress proxy returns a hard policy denial for every data-source and live-site domain the CEO needs: `eia.gov`, `api.eia.gov`, `imf.org`, `portwatch.imf.org`, `oilthroughhormuz.com`, `wp-tszabo.github.io`, and the GitHub Pages API path. Consequence: (a) no data source's ToS can be read in full, so none can be cleared — search snippets indicate IMF PortWatch is under the IMF's bespoke Data Terms (2024-10-11), explicitly not Creative Commons; (b) rubric §1's spot re-fetch is impossible, so the CEO cannot verify a published figure or detect a stale feed. No confirmed outage: DNS for the apex resolves to the four GitHub Pages IPs, `www` CNAMEs to `wp-tszabo.github.io`, and the last Pages deploy (`3fd9200`) succeeded 2026-09-16. | Paused all data wiring; cleared no source; published nothing (no commit touches `site/`, so no deploy fired). Candidate sources recorded as UNVERIFIED in `decisions-log.md`. Continued only the in-bounds part of the approved plan (copy drafting, held for review). Opened GitHub issue #1, label `critical`, with three options for the owner (allowlist / CI-side fetch+verify / accept the pause). | **MOSTLY RESOLVED 2026-09-17 (2nd cycle)** — egress restored without owner action; see the two rows below, which supersede this one. Issue #1 left open for the residual asks (CI-side verification; product re-scope decision). | 2026-09-17 (egress condition only) |
| 2026-09-17 (2nd cycle) | 3 (legal/compliance) — **resolved, negative** | Egress restored (all of `eia.gov`, `portwatch.imf.org`, `imf.org`, `wp-tszabo.github.io` returned HTTP 200 on re-test), so the ToS read blocked above was finally possible. **Result: IMF PortWatch is NOT redistributable for this product.** Every PortWatch dataset carries `license: custom` with `licenseInfo: https://www.imf.org/external/terms.htm` (verified first-hand via the ArcGIS Hub item metadata, not from search snippets). Those IMF terms permit only "free non-systematic downloading … for personal, noncommercial usage only without any right to resell, redistribute, compile, or create derivative works", and require permission "to copy or download IMF Content in any systematic way". An automated daily fetch → store → re-publish is squarely inside all four prohibited verbs and outside the narrow 1,000-word Fair Use carve-out. EIA, by contrast, verified clear: US government work, public domain, free to use and distribute with acknowledgement — but its Hormuz chokepoint release is annual/half-yearly and its freshest published figure (20.9 m b/d, 1H25) covers a period that ended ~14 months ago, while Q1-2026 flows are reported ~30% lower amid regional conflict. | **Nothing ingested, nothing wired, nothing published** — the guilty-until-checked rule caught this before any code depended on it. PortWatch recorded as REJECTED in `decisions-log.md`. Posted the full verdict plus three re-scope options to issue #1 (A: ask `copyright@imf.org` for written permission, $0; B: re-scope honestly around EIA's real vintage; C: Phase 2 paid-data conversation — explicitly **not** recommended until A is tried). **No email sent to the IMF** — outward-facing contact on the company's behalf is the owner's to authorise. | OPEN — awaiting owner decision on re-scope (issue #1) | — |
| 2026-09-17 (2nd cycle) | 1 (site not serving correctly) | **HTTPS is broken on the custom domain.** `https://oilthroughhormuz.com/` fails TLS: the edge presents `CN=*.github.io` and `subjectAltName does not match oilthroughhormuz.com`. `https://www.oilthroughhormuz.com/` fails the same way, and `http://www.…` 301-redirects *into* the broken HTTPS URL. Only `http://` on the apex (HTTP 200) and `https://wp-tszabo.github.io/oil-through-hormuz/` (HTTP 200) serve correctly. Cause: Pages has the custom domain configured but the Let's Encrypt certificate was never provisioned — the classic symptom of DNS landing after the domain was saved. **This was previously sitting in `backlog.md` as a routine "turn on HTTPS enforcement" task; it is not routine, it is the primary domain being broken for every normal visitor, so it is re-raised at critical severity.** Note the earlier egress block had been masking a genuine outage: "can't fetch our own site" was read as a proxy rule, and was in fact partly a real defect. | Opened GitHub issue #3, label `critical`. **Did not touch DNS or `site/CNAME`** — domain changes are a named hard-to-reverse action under GOVERNANCE.md. The Pages REST API path is blocked by the sandbox proxy (`Access to this GitHub API path is not permitted through this proxy`), so the CEO can neither read cert state nor toggle Enforce HTTPS; this needs the owner in **Settings → Pages** (clear + re-save the custom domain to re-trigger issuance, then tick Enforce HTTPS). `metrics.md` uptime recorded as **DEGRADED**, not up. | **RESOLVED 2026-09-17 (3rd cycle)** — owner cleared and re-added the custom domain in Settings → Pages, which unstuck certificate issuance. **Verified by the CEO before closing, not accepted on report**: `https://oilthroughhormuz.com/` now presents `CN=oilthroughhormuz.com` issued by Let's Encrypt (expires 2026-12-16), `SSL certificate verify ok`, HTTP 200 — and all four entry routes (`http://` apex, `https://www`, `http://www`, the `github.io` URL) now terminate on the valid-TLS apex, with HTTPS enforcement on (plain HTTP 301s up rather than serving). GitHub issue #3 closed with that evidence. `metrics.md` uptime moves DEGRADED → UP. **Lesson carried forward rather than just closed**: nothing in this company would have detected this on its own — it was caught by hand, and only because egress happened to be working that hour. The CI-side monitor must assert *certificate validity*, not just HTTP 200; an HTTP-only check would have reported green for the whole outage. | 2026-09-17 |
| 2026-09-17 (4th cycle) | 4 (hard-to-reverse — misrepresenting the data's authority) — **RESOLUTION of the pre-emptive issue #5 logged above** | Resolution row, not a new issue. The owner answered the escalation on PR #4 (comment `5716791659`, 15:16:58Z): *"The daily figure is the estimate. That is the real value of the whole page thta we develop a model that make that estimate. So let's add 'estimated ' keyword to the title but let's keep the daily figure."* That overrules the CEO's recommendation to headline the measured quarter, and it is exactly the decision the escalation asked for. | Implemented on `copy-review/homepage-v2`, **not merged**: headline is now the model's point estimate for 2026-09-16 (4.9 m b/d), with "estimate" in the `<title>`, the H1, the unit line and a standalone "Estimate — not a measurement" label, and the **1.5–6.9 m b/d band shown directly under the figure**. The band was not part of the owner's instruction; `rubric.md` §1.6 requires a visible range for any modelled figure and the owner was not asked to waive it, so both ship together — see `decisions-log.md` for that interpretation stated plainly enough to be challenged. Rubric re-run: **PASS** (`self-check-log.md`, 4th-cycle row), including a fresh EIA spot re-fetch and an explicit §3.2 re-read of the Terms and Copyright paragraphs, which had called the figures "third-party estimates" and stopped being true once the headline became our own model output. | **RESOLVED — the category-4 exposure is closed** (the page no longer asserts a measurement it cannot support). Publication remains gated: the owner resolved the content question without saying "merge", so the branch is updated and final merge approval is requested on PR #4. **One new, lower-severity risk created by the decision and recorded rather than absorbed**: the estimate date is hard-coded, so the page goes stale by construction — not a category-1 issue on publication day, but it becomes one if nothing re-dates it. Now the top Build item. | 2026-09-17 |
| 2026-09-17 (5th cycle) | 4 (hard-to-reverse / misrepresenting the data's authority) — **assessed, resolved in design, NOT raised as an interrupt** | Logged for the record because the assessment was real, not because an interrupt fired. The owner directed that the visible trend/chart copy not name an exact source, on the grounds that the product is a proprietary aggregation method rather than a pass-through of one provider. The company today has **exactly one** licence-clear numeric input. A literal maximal reading of that directive — remove the attribution and let "our proprietary model" stand alone — would imply either that we observe the strait ourselves or that we already blend many feeds. Both are false, and publishing either would be category 4 and a rubric §1.1/§3.2 FAIL. So the directive was treated as something to resolve carefully rather than execute literally. | **Resolved in design, nothing published.** The visible primary copy speaks only of "our model"/"our method"; the full attribution (publisher, dataset, table, release date, licence, Vortexa provenance disclaimer, the publisher's own AIS-reliability caveat, and the daily trackers checked and rejected on licence) moved **intact** to a new linked `sources.html`, reachable from the chart caption, the methodology blurb and the footer. `index.html` grep-verified to contain zero occurrences of EIA / Energy Information Administration / Vortexa / PortWatch / AXSMarine / WTO; `sources.html` carries all of them. No extra input diversity is claimed anywhere: `sources.html` states "One cleared dataset, and we say so rather than implying a wider net", and the aggregation ambition appears only as a "Where this is going" section describing ongoing work. Rubric re-run in full: **PASS**. Held on `copy-review/homepage-v3` and put to the owner as **PR #6**, with the reasoning written out on the PR so the owner can overrule it. | **RESOLVED IN DESIGN — no interrupt raised, and one stated in advance if the position changes.** The threshold is explicit: if the owner asks for *no* visible pointer to the source at all, the CEO will raise that as a category-4 critical issue **before** implementing it, rather than complying against its own judgement. That version crosses into misrepresenting the data's authority; the version shipped does not. Recorded here so the line is on the record before the conversation rather than after it. | 2026-09-17 |
| 2026-09-18 (6th cycle) | 1 (site down / data stale — **fired, real, predicted**) | **The live estimate date was frozen at 16 September while the page was served on 18 September.** `https://oilthroughhormuz.com/` headlined *"Our model's estimate for Wednesday 16 September 2026 — 4.9"*. The date is hard-coded in `site/index.html` line 53 and `site/data/hormuz.json` `model.estimate.for_date`, and nothing re-dates it. This is worse than a cosmetically old date, because the methodology section makes an explicit promise about it: *"Our model ... calibrates against the quarterly record, and **carries it forward to the day you are reading**."* That sentence was **false as served** — a visitor on 18 September was not being shown a figure carried forward to the day they were reading. **This was predicted on the day it was created**: the 4th cycle recorded that the hard-coded date was "not a category-1 issue today ... but it becomes one if nothing re-dates it." Nothing re-dated it, so it became one. Logged as a genuine fired interrupt rather than absorbed as routine maintenance, because the whole point of that prior note was that the next cycle should treat it as category 1. | **Raised first, before any other cycle work, as GitHub issue #7 (`critical`).** Then applied the minimal truth-restoring fix: re-dated to Thursday 17 September 2026 in both files. **Deliberately changed nothing else — no prose, and no figure.** The shape function is persistence (`shape(d)=1`), so the point estimate is 4.9 for every day past the 2Q26 anchor, and 16/17/18 September are all within the same one-quarter-ahead band horizon; 1.5–6.9 is unchanged. Reasoning recorded in the issue so the owner can reverse it: leaving a knowingly false sentence live was judged the worse of the two errors, and a date that tracks the reader's day is the design the owner **already approved** on PR #4 — a frozen date is a defect against that design, not an editorial choice the CEO made. GOVERNANCE.md explicitly contemplates publish-then-verify where waiting is not tenable; the rubric was re-run (PASS) and logged. **The durable fix was NOT self-approved**: `scripts/refresh_estimate.py` + a daily Actions cron, with a horizon guard, opened as **PR #8** and held, because standing up an unattended publisher of live content is an owner decision, not a CEO implementation detail. | **RESOLVED 2026-09-19 (8th cycle) — durably, and verified end-to-end rather than declared.** The owner approved the design PR #8 was blocked on (*"one quarter is fine for now, we should extend it later"*) and the CEO merged it (`160498a`). Two plumbing defects were found in pre-merge re-verification and fixed first (`82d6e86`) — most importantly, the bot's push would **not** have triggered the Pages deploy (GitHub does not fire workflows from `GITHUB_TOKEN` pushes), so the automation would have gone green while the live page stayed frozen: this exact issue, wearing a disguise. Proof it now works: refresh run `35457521885` green → bot commit `3ecd690` → deploy run `35457530338` green → live page re-fetched byte-identical to `main`, headline "Friday 18 September 2026" on 19 September. GitHub issue #7 closed on that evidence. **Residual, tracked not hidden**: the first *unattended* cron run (05:10 UTC 2026-09-20) has never executed, and verifying it (green run, bot commit, and a deploy fired from it) is a backlog item. | 2026-09-19 |
| 2026-09-18 (6th cycle) | 4 (misrepresenting the data's authority) — **assessed pre-emptively, NOT yet fired, flagged to the owner with a date on it** | Logged because it has a known expiry date, not because it is live today. The published band (1.5–6.9) is derived from a back-test that only measures how wrong persistence gets **one quarter past its anchor** (`methodology.md` §4). The current anchor covers through **2026-06-30**. One quarter past that is **2026-09-30 — twelve days from this cycle.** From that date, absent a new EIA release (not expected until ~November), every day the site keeps publishing "4.9, range 1.5–6.9" is an extrapolation beyond anything the company has ever tested, presented with a range that implies it was tested. That is the same class of claim-beyond-evidence as category 4, and it arrives on a schedule. | **Not left to be discovered.** Added a horizon guard to the model (`MAX_HORIZON_DAYS = 92`): past the back-tested horizon the point estimate is **suppressed entirely** and the page says "No current estimate — awaiting the next published quarter" rather than stretching an untested range. Implemented and unit-tested in `scripts/refresh_estimate.py`, documented in `methodology.md` §12, described in the `sources.html` copy under a new "When we stop answering" heading. **Held in PR #8, not merged** — and called out explicitly at the top of the PR, because merging it means the site's headline number most likely disappears at month end, which is a product decision the owner must make knowingly rather than inherit from a safety mechanism the CEO installed quietly. | **RESOLVED 2026-09-19 (8th cycle) — by owner decision, exactly the way this row asked for.** Owner, live in conversation: *"one quarter is fine for now, we should extend it later."* The threshold was **not** changed: `MAX_HORIZON_DAYS` stays at 92, and from ~2026-10-01 the page will show "No current estimate — awaiting the next published quarter" until EIA publishes 3Q26. The owner accepted the blank headline knowingly, which is the entire reason this was escalated instead of installed quietly. Merged live in PR #8 (`160498a`); the behaviour is described to readers in advance under `sources.html` → "When we stop answering". **"Extend it later" is recorded as future work, not as authorisation**: `methodology.md` §14.2 and an `okrs.md` KR6 sub-goal, with the bar written down before it is under pressure — a longer horizon must be earned by a measured error at that horizon, never by the awkwardness of a blank number. **One smaller thing this row did not anticipate, now carried separately**: in the suppressed state the sentence below the generated block still reads "The range is this wide because…". Incoherent, not untrue, so not a critical issue — held for owner copy review at `pending-copy/2026-09-19-suppressed-state-note.md`. | 2026-09-19 |
| 2026-09-19 (7th cycle) | 1 (site down / data stale — **fired again, second consecutive day, same defect**) | **Recurrence of issue #7.** The live estimate date was frozen at **17 September** while the page was served on **19 September**. Verified by direct fetch before any other cycle work: HTTP 200, `ssl_verify_result=0`, served bytes identical to `main`, headline *"Our model's estimate for Thursday 17 September 2026"*. As on 18 September, the defect is not a cosmetically old date — the methodology section promises the reader a figure *"carried forward to the day you are reading"*, and that sentence was false as served for the second day running. **The significant fact is not the recurrence but what it proves**: yesterday's hand-fix decayed within 24 hours, so manual re-dating is not a control at all — it only works on days a CEO cycle happens to run, and fails silently otherwise. | **Raised first, before any other work**, as a comment on the still-open issue #7 framing it explicitly as a treadmill rather than a resolved defect. Then applied the same minimal truth-restoring fix: re-dated to **Friday 18 September 2026** in `site/index.html` and `site/data/hormuz.json`. **No prose changed, no figure changed** — `shape(d)=1` (persistence) so the point estimate is 4.9 for any day past the 2Q26 anchor, and 18 Sep is 80 days past the anchor end, inside the 92-day back-tested horizon, so the 1.5–6.9 band is untouched. Source spot re-fetched as part of the rubric run: EIA supplement release still "August 12, 2026", Table 4 series identical. Commit `e4adf11`, pushed, deploy verified live (`for_date: 2026-09-18` served). Rubric re-run: pre-fix live page **FAIL**, post-fix **PASS**. **The durable fix was again NOT self-approved** — PR #8 remains held and unmerged, because standing up an unattended publisher of live content is the owner's decision no matter how many times the manual alternative fails. | **OPEN** — manual fix live; automation still held in PR #8, which has had no owner response since 2026-09-18. Issue #7 open. | — |
| 2026-09-19 (7th cycle) | 4 (misrepresenting the data's authority / claim beyond evidence) — **raised as GitHub issue #9, live figure NOT changed** | **A newly cleared input contradicts the number the site is publishing.** This cycle's standing KR6 pass cleared **EIA STEO Table 3d** (monthly crude production by country, US public domain, same publisher and terms as the existing anchor, observed history through **2026-08** — two months fresher than the Hormuz anchor) and built **GPCI** from it. The flow/GPCI ratio is tight in calm quarters (1.070–1.085, sd 0.006) and collapses to 0.410 in 2026Q2. **2026Q3 GPCI is 14.42, up 20.6% on the Q2 trough.** Applying every transit ratio ever observed to that gives implied Q3 Hormuz flows of **5.9 / 11.8 / 15.5 m b/d — every one above the published point estimate of 4.9**, and the lowest of them sitting at the very top of the published 1.5–6.9 band. The page tells readers to "treat the range as the answer"; on this evidence the bottom half of that range is supported by nothing, and persistence-at-the-trough is no longer a neutral assumption. That is a claim-beyond-evidence problem and it is category 4. | **Raised to the owner as issue #9 (`critical`) with four options; nothing changed on the live site.** The CEO did not re-fit the model, did not widen the band, and did not touch the headline: changing a published figure requires the owner, a fresh rubric run and the copy checkpoint, and this derivation is one cycle old with no specialist review. The counter-argument was put on the issue as prominently as the finding — the transit ratio is *unstable in disruption*, which is the finding itself, so a low Q3 ratio (~0.30) would put the truth back inside the current band; the direction of the evidence is solid, the magnitude is not. Options offered: (A) change nothing, (B) widen the band upward keeping the point estimate, (C) re-anchor the model on GPCI, (D) suppress the estimate now rather than at the 30 Sep horizon. **CEO recommendation: B now, C after review.** The copy describing the new input was drafted and **held** at `pending-copy/2026-09-19-sources-v3-gpci.md`, marked conditional and explicitly not to be banked if the owner declines — because `sources.html` promises to describe what is in the model *today*, and an analysed-but-unused input listed there would overstate input diversity, which is itself category 4. Full working in `methodology.md` §13. | **OPEN — owner decision required.** Note this finding independently strengthens the case for PR #8's horizon guard: it is direct evidence that persistence drifts away from reality past the anchor, which is what the guard exists to stop. | — |
| 2026-09-20 (9th cycle) | 1 (site down / data stale) — **assessed against the live page, NOT raised, with a trigger written down** | **The opening check found the live headline reading "Friday 18 September 2026" at 02:20 UTC on Sunday 20 September** — two calendar days back, where the design is one. Verified by direct fetch: HTTP 200, `ssl_verify_result=0`, served bytes **identical** to `main` (`e2c812e`), `data/hormuz.json` 200. Source spot re-fetched: EIA supplement release still **12 August 2026**, Table 4 still `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9`, so the figure itself is correct and current — the question is only the date label. **Why this is not the third firing of issue #7, stated so the reasoning can be challenged rather than assumed:** the refresh job computes `for_date = today(UTC) − 1` and runs at **05:10 UTC**. Between 00:00 and 05:10 UTC every day, the page therefore carries the day *before* yesterday. The last run (manual, 2026-09-19 17:15 UTC) correctly wrote 18 September; the next run is at 05:10 UTC today, about 2h50m after this check. So this is a **bounded, self-correcting, ~5-hour daily window**, not a frozen date with no mechanism behind it — which is the exact property that made 18 and 19 September category 1. **What is genuinely wrong, and is not being waved away:** the workflow's own comment claims 05:10 UTC is "after midnight everywhere that matters", and for the Strait of Hormuz that is backwards — the Gulf is UTC+3/+4, so local midnight is 20:00–21:00 UTC the *previous* day, and a Gulf reader sees a two-days-back date for roughly nine hours each night. The page's prose promises a figure "carried forward to the day you are reading", which is the sentence that turns a cosmetic lag into a truth problem. There is no cron time that is correct for every reader; the honest fix is to label the date as UTC in the copy. | **Nothing touched on the live site, deliberately, for three separate reasons — any one of which would have been enough.** (1) The week-of-2026-09-21 plan is still `PROPOSED` (issue #10, no owner response), so the company is read-only: no publishing. (2) Hand-re-dating is the "control" this company already proved is not a control (issue #7, 2nd occurrence, 2026-09-19). (3) **Most important: hand-setting the date to 19 September would have destroyed the evidence.** The 05:10 UTC run today is the first *unattended* cron fire in the company's history; if the CEO pre-writes the answer, the job finds nothing to change, commits nothing, and the one test that matters returns a false green. Manually dispatching the workflow was rejected for the same reason. Instead: a copy fix labelling the date as UTC is drafted and **held** at `pending-copy/2026-09-20-date-semantics.md`, and a proposed cron-time change is a backlog item for the owner to decide with next week's plan. | **ASSESSED — NOT RAISED. Trigger recorded in advance, so the next cycle does not have to re-argue it:** if the live `for_date` is still `2026-09-18` after 05:10 UTC on 2026-09-20, the automation has failed its first unattended run and that **is** category 1 — raise it immediately as a new GitHub issue, do not reopen this row. Equally, if the date is ever more than one day behind the reader's UTC date *outside* the 00:00–05:10 window, that is category 1. Verifying today's cron run (green run **and** a bot commit **and** a deploy fired from it) is the first item of the next cycle. | — (assessment, no interrupt) |
| 2026-09-21 (10th cycle) | 1 (site down / data stale) — **assessed against the live page, NOT raised; and the trigger written down last cycle turned out to be miscalibrated, which is recorded here rather than quietly fixed** | **Opening check, before any other work.** Live page: HTTP 200, `ssl_verify_result=0`, served bytes **identical** to `main` (`9a1d712`), `data/hormuz.json` 200. Headline reads *"Saturday 19 September 2026"* at **02:20 UTC on Monday 21 September**. Source spot re-fetched: EIA supplement release still **12 August 2026**, Table 4 unchanged, so the figure is correct and current. **The residual that nine cycles carried is now closed — positively.** The first *unattended* scheduled run is verified on all three legs the plan demanded: refresh run **`35503239041`, `event=schedule`, conclusion `success`** → bot commit **`9a1d712`** re-dating the estimate → Pages deploy **`35503243978`, `success`, head `9a1d712`** → live bytes match. KR2's "auto-updating" half is met unattended for the first time. **But the run fired at 09:46:34 UTC, not the 05:10 UTC the cron requests — 4h36m late.** GitHub Actions schedules are best-effort under load. Three consequences, none of which is a live failure today but all of which were unknown when last cycle's trigger was written: (1) the "bounded, self-correcting ~5-hour window (00:00–05:10 UTC)" recorded on 2026-09-20 is **not the real window** — on 20 September it was ~9h46m, and it has no guaranteed upper bound at all; (2) the pre-written trigger *"more than one day behind the reader's UTC date outside the 00:00–05:10 window is category 1"* **would have misfired** had this cycle run at 06:00 UTC on 20 September, declaring a critical issue over ordinary scheduler latency; (3) far more seriously, **GitHub can skip a scheduled run entirely, and nothing would go red** — `refresh_estimate.py` fails loudly only when it *runs* and the fetch or parse fails. Silent non-execution is currently undetected except by a CEO cycle happening to look, which is the same "manual checking is not a control" lesson that issue #7 already taught once. | **Nothing touched on the live site, deliberately.** The page is within its documented design tolerance right now (02:20 UTC is inside even the narrow window), the figure is correct, and the week-of-2026-09-21 plan is still `PROPOSED` (issue #10, no owner response), so the company is read-only: no publishing, no hard-to-reverse action. **Not raised as an interrupt**, because GOVERNANCE.md category 1 is "the tracker isn't showing current, correct data" and it is showing current, correct data — what is defective is our *monitoring* of it and our own trigger's arithmetic, neither of which is a live misstatement to a reader. **Trigger corrected here, in advance, so the next cycle inherits the right one rather than last cycle's:** a two-days-back date is category 1 **only if it persists past ~12:00 UTC**, or if **no `event=schedule` refresh run has succeeded in the preceding 36 hours** — the second condition is the one that catches a silently skipped cron, which the old trigger could not. **Escalated into the plan rather than patched around**: the week's Build item (an independent uptime + certificate monitor) is hereby widened — it must also assert **data freshness** (`for_date` within N days of now) and **that the scheduled job actually ran**, not merely HTTP 200 and a valid certificate. An HTTP-only check would have reported green through the 16-hour TLS outage of 2026-09-17; an HTTP+TLS check would report green through a frozen page forever. That widening needs the plan approved before it can be built and deployed. The pending copy draft `pending-copy/2026-09-20-date-semantics.md` also becomes more load-bearing than when it was written: the daily window it exists to explain is roughly twice as long as we thought. | **ASSESSED — NOT RAISED. The 2026-09-20 residual is RESOLVED** (first unattended scheduled run verified green end-to-end). **One new open item, owner-gated**: the freshness/liveness monitor cannot be built until the week-of-09-21 plan is approved. | 2026-09-21 (residual closed); monitor open |
| 2026-09-22 (11th cycle) | 1 (site down / data stale) — **checked first, nothing found, NOT raised** | **Opening check, before any other cycle work, run against the corrected trigger from 2026-09-21 rather than the miscalibrated one.** Live page `https://oilthroughhormuz.com/`: HTTP 200, `ssl_verify_result=0`, valid Let's Encrypt cert (CN=oilthroughhormuz.com, expires 2026-12-16), served bytes **byte-identical** to `origin/main` (`821eb9f`) for both `index.html` and `data/hormuz.json`. **The trigger that actually matters — "no successful `event=schedule` refresh run in 36 hours"** — checked against the Actions API, not inferred: last scheduled run `35589882310` (`event=schedule`, `success`) at **2026-09-21T10:39:14Z**, ~15h41m before this check, → bot commit `821eb9f` → deploy `35589896792` (`success`). Inside the window; today's 05:10 UTC cron has not fired yet (check ran at 02:20 UTC). **Estimate date reads 2026-09-20 at 02:20 UTC on 2026-09-22 — two days back, and that is the known 00:00–05:10 UTC design window, not a frozen date**; it was deliberately **not** hand-fixed, for the same reason as the last two cycles (a hand-fix returns a false green on the automation test). **Scheduler latency now has three observations and a pattern**: cron requests 05:10 UTC, actual fires 09:46, 09:46, 10:39 — consistently ~4.6–5.5h late, never early. Recorded because it means the honest worst-case stale window is ~10–11h, not the ~5h originally assumed, and the pending date-semantics copy draft is the right place to tell readers so. **Source spot re-fetch (rubric §1.2)**: EIA supplement re-fetched, release still 2026-08-12, Table 4 total-oil row still `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9`, identical to `hormuz.json`. No category-1 issue. | **Not raised — no issue found.** Logged anyway so the unattended check is auditable. | CLOSED (no issue) | 2026-09-22 |
| 2026-09-22 (11th cycle) | 3 (legal/compliance — data source terms) — **pre-emptive, NO exposure incurred, source NOT adopted** | **A strong new candidate input has a licence that is only half-clear, and the unclear half is the half Phase 2 would land on.** This cycle's KR6 pass evaluated **Eurostat `nrg_ti_oilm`** (EU27 crude imports by partner country, monthly) — see `methodology.md` §17. Terms read first-hand at `ec.europa.eu/eurostat/web/main/help/copyright-notice` (HTTP 200), Exceptions section included. The general grant authorises reuse "for commercial or non-commercial purposes… provided the source is acknowledged." **But the Exceptions carve out, as non-commercial-only, "Data for countries other than: Member States of the European Union (EU), Member States of the European Free Trade Association (EFTA), official EU acceding and candidate countries."** Saudi Arabia and Iraq are outside that list. Whether an EU-declared import series *broken down by partner* counts as "data for" the partner is genuinely ambiguous; the notice's own Switzerland/Austria clarification points to the **declarant** reading (it permits selling "Swiss export/import data declared by an EU Member State"), which would make our use commercial-safe — **but that is our reading of their text, not a grant, and an inferred permission is not a permission.** Verdict recorded: **CLEARED for non-commercial reuse without restriction; AMBIGUOUS for commercial reuse.** **The real risk is a silent flip, not today's use**: the site is non-commercial today, so the permissive half covers it; if this input were adopted now and the owner later approved Phase 2 monetization, the licence status of a *live* input would change with no alert anywhere in the system. | **Source NOT adopted, and the analysis deliberately kept where it cannot matter.** It does not enter `site/data/hormuz.json`, does not touch the published 4.9 / 1.5–6.9, and `sources.html` was **not** updated (listing an analysed-but-unused source would overstate input diversity — category 4). Analysis lives in `scripts/eu_imports_analysis.py`, marked ANALYSIS ONLY in its docstring. **Judgement recorded so it can be audited and overruled: no new `critical`-labelled GitHub issue was opened.** Reasoning — no exposure has been incurred, nothing is published, the action is already paused by the read-only state, and two interrupts (#9, #10) have been open and unanswered since 2026-09-19/20, so a third would dilute the channel rather than sharpen it. Instead it is surfaced as an explicit owner decision on the week-of-21 plan and posted to issue #10. **If a future cycle is ever tempted to adopt this input, that temptation is the moment this becomes a real interrupt.** | **OPEN — needs owner decision.** Two questions: (a) adopt as a non-commercial-only input, accepting that it constrains Phase 2; (b) hold it out entirely until the commercial question is settled in writing. CEO recommends (b): it adds corroboration, not forward information, so the cost of waiting is low and the cost of an entangled live input is not. | — |
| 2026-09-23 (12th cycle) | 1 (site down / data stale) — **checked first, nothing found, NOT raised** | **Opening check at 02:17 UTC, before any other work, against the corrected 2026-09-21 trigger.** Live `https://oilthroughhormuz.com/`: HTTP 200; `index.html` and `data/hormuz.json` **byte-identical** to `origin/main` (`0622a81`). **Liveness trigger** (no successful `event=schedule` run in 36h), checked from the Actions API: last scheduled refresh `35713023984` succeeded **2026-09-22T09:54:26Z** (~16h23m before the check) → bot commit `0622a81` → deploy `35713037884` (success). Fourth scheduled observation: cron 05:10, fired 09:54, so latency is still ~4.6–5.5h late and never early. `for_date` reads 2026-09-21 at 02:17 UTC on 09-23, which is inside the documented window. Not hand-fixed. **Source re-fetch**: EIA supplement HTTP 200, Table 4 Hormuz row still `20.9 / 21.0 / 21.3 / 21.6 / 14.9 / 4.9`, identical to `hormuz.json` (the page banner reads "September 9, 2026", which is the parent STEO, the known trap, deliberately not used). **A change in what this check can observe:** this cycle's sandbox routes HTTPS through an egress proxy that re-signs certificates (issuer `Anthropic Egress Gateway SDS Issuing CA`), so `ssl_verify_result=0` proved **only the proxy chain, not the origin certificate**. Prior cycles' "valid Let's Encrypt, expires 2026-12-16" was a direct observation; this cycle's is not. Origin cert state was instead confirmed from Certificate Transparency (crt.sh: Let's Encrypt `YR1`, valid 2026-09-17 → 2026-12-16, no newer or revoked issuance seen). The GitHub Pages API (`/pages`, which reports `https_certificate` state) is **blocked by the proxy** ("Access to this GitHub API path is not permitted"). | **Not raised: no live failure.** Recorded because it **strengthens the case for the owner-gated monitor**: the CEO's own TLS check can silently degrade to checking a proxy, which is the same "the check passes while not checking the thing" failure the monitor item was widened to prevent. The monitor must run from infrastructure that sees the origin certificate (e.g. a GitHub Actions job), not from the CEO sandbox. | CLOSED (no issue) | 2026-09-23 |
| 2026-09-23 (owner response) | 4 — **RESOLUTION of critical issue #9** | Resolution row, not a new issue. The owner closed issue #9 at 2026-09-23T10:34:58Z: *"Let's go with option C. In the future let's make sure that the CEO has authority to decide on the used methodology."* | **Implemented and held, not published.** The CEO re-anchored the model on the GPCI transit-share estimator (C as the issue body defined it; interpretation and choice over C′ in `methodology.md` §19). The band is now derived from the estimator's own back-test: **5.6, range 4.9–10.8**, replacing 4.9, range 1.5–6.9. The bottom of the old range, the part #9 said nothing supported, is gone. Rubric PASS. Opened as **PR #11** (`needs-copy-review`), **not self-merged**. The methodology grant was written into GOVERNANCE.md. | **RESOLVED as a decision; publication pending owner approval of PR #11.** The live figure stays 4.9 / 1.5–6.9 until the PR merges. Either way the headline blanks from ~2026-10-01 under the unchanged horizon guard. | 2026-09-23 (decision) |
| 2026-09-23 (owner response) | 1 (data feed would go silently stale) — **pre-emptive, fix held in PR #11** | **The live refresh job cannot re-anchor on a new EIA quarter, and would fail silently.** `scripts/refresh_estimate.py` on `main` parses Table 4 by position, taking the first six numbers per row. Reproduced against `main`'s code with a synthetic seven-column table: the new 3Q26 value is ignored, the job prints `OK`, and no re-anchor happens. From ~2026-10-01 the headline is suppressed past the horizon. When EIA publishes 3Q26 (~November), the job would keep reporting green while the page stayed blank indefinitely. A "frozen page, green build" is the exact disguise of issue #7 that the 2026-09-21 monitor work warned about. If EIA instead *rolls* the table, the old code writes each value under the wrong quarter label. Separately, any EIA revision while suppressed crashes the job (`TypeError`, `None / float`); that is loud, which is the better failure. | **Fixed in PR #11**: period-label parsing, a row whose value count differs from the header is refused, and suppression-safe scoring. Edge paths tested (see self-check row). **No separate GitHub issue opened**: nothing is failing today, the fix is already in front of the owner in PR #11, and the PR body calls it out with the date that matters. | **OPEN until PR #11 merges.** Trigger written in advance: **if PR #11 is declined or still unmerged when EIA's supplement next changes, split the parser fix into its own PR immediately and raise it as `critical`.** | — |
